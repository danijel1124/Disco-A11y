# Disco-A11y — TODOs

- **Disco Elysium (Disco-A11y) — Hotkey-Problem behoben, eigener Fork + Release + Editor-Tool gebaut**
  Ursprüngliche Einschätzung ("hängt hinterher") war ein Trugschluss auf Basis der reinen Spielerfahrung: Repo ist inhaltlich das reifste der drei Mods (90 Commits, Sept 2025 – April 2026, voller Funktionsumfang: Navigation, Dialog, Inventar, Journal, Thought Cabinet, Charakterbogen, Waypoints, Braille, RTL-Sprachen). Eigentliche Ursache war rein technisch: Hotkeys hartcodiert auf Unitys physische US-QWERTY-`KeyCode`s (`[`, `]`, `\`, `;`, `'`, `` ` ``) — auf deutschem QWERTZ-Layout an anderer Stelle bzw. nur über AltGr erreichbar (kollidiert mit Unitys Ctrl+Alt-Meldung für AltGr).
  Repo-Herkunft: `game-a11y/Disco-A11y` (kein eigenes Release je veröffentlicht — nur Sourcecode + lokales `release.sh`), Autor hat mit Claude Code gearbeitet (Commit-Trailer bestätigt).

  **Umgesetzt (committed und gepusht zu Fork `github.com/danijel1124/Disco-A11y`, Commits `6340e4a` + `45b6163` auf `main`):**
  1. Eigenen Fork erstellt (`gh repo fork`), lokalen Remote `fork` hinzugefügt.
  2. MelonLoader lokal installiert + Spiel einmal gestartet (Il2Cpp-Assemblies generiert), Mod lokal gebaut (`net6.0`, braucht `DISCO_ELYSIUM_PATH`), Tolk-Binaries (aus `SoniFight` vendored) ergänzt, Release `v0.1.0` gepackt und auf den Fork veröffentlicht (`gh release create`) — sauber ohne jede projektfremde Erwähnung (siehe Memory-Regel dazu).
  3. **Tastenbelegung konfigurierbar gemacht**: neues `GameKey`/`KeyBindings`-System (`mod/Settings/`), persistiert in `UserData/AccessibilityMod.cfg`. `InputManager.cs` liest jetzt über `KeyBindings.IsPressed(...)` statt hartcodierter `Input.GetKeyDown`-Aufrufe. Zwei Presets: `Defaults` (Original-US-Belegung) und `NumpadSafePreset` (layoutunabhängig).
  4. **stardew-access als Vorbild übernommen** (`stardew-access`, `docs/keybindings.md`): vier Aktionen 1:1 auf dessen Konvention umgestellt statt Ziffernblock, weil layoutsicherer *und* auch ohne Ziffernblock (Laptop) nutzbar: `Escape` = Bewegung stoppen, `Bild↑`/`Bild↓` = Kategorie zurück/weiter, `Strg+Pos1` = zum Objekt gehen. `H` (Status/Gesundheit) war schon identisch zu stardew-access belegt.
  5. **Eigenständiges GUI-Tool `tools/KeybindEditor`** (WinForms, .NET 8): Spielordner wählen, alle Tasten per Liste einsehen/frei neu belegen, Presets laden, Dialogmodus/Orb-Ansagen/Sprachunterbrechung mit einstellen, speichert direkt in die echte Config. **Lokalisiert** (Englisch/Deutsch, umschaltbar, Standard nach OS-Sprache). Screenreader-Fixes nach Live-Test: Speichern wird jetzt per Tolk *und* MessageBox angesagt (vorher stumm), Tastenkombination wird jetzt zusammen mit der Aktion in einer Zeile angesagt (vorher hat NVDA nur die Aktion vorgelesen, nicht die zweite Spalte). Exe: `Disco-A11y/tools/KeybindEditor/bin/Release/net8.0-windows/win-x64/publish/DiscoElysiumKeybindEditor.exe`.
  6. Live im echten Spiel getestet (mehrfach neu gestartet, Log geprüft): Config lädt/speichert fehlerfrei, Tolk spricht, neue Tasten laden ohne Fehler.
  7. Zwei Nebenbugs beim Live-Testen gefunden und gefixt: (a) `MelonPreferences` schreibt die Cfg-Datei nur bei explizitem Save, nicht automatisch bei `CreateEntry` — jetzt expliziter `SaveToFile()`-Aufruf beim Initialisieren. (b) `Start-Process` ohne `-WorkingDirectory` lässt das Spiel mit falschem Arbeitsverzeichnis starten, wodurch der relative Cfg-Pfad ins Leere lief.
  8. `deps/` im ganzen Gesamtprojekt in `` umbenannt (passender Name, da es keine echten Build-Dependencies sind, sondern Referenzprojekte).

  9. **Eigenständiger Installer gebaut** (`tools/Installer`, WinForms, unabhängig von `AccessModsInstaller` — das gehört zum AccessMods-Projekt und wird nicht angefasst/erweitert, siehe Memory-Regel dazu): erkennt/fragt Spielordner, installiert MelonLoader (gleiche dynamische Versions-/Architektur-Erkennung wie oben) + Mod (Download vom Fork-Release, da Upstream nie eins veröffentlicht hat). Zusätzlich dauerhafter nicht-interaktiver CLI-Modus (`DiscoElysiumInstaller.exe --cli [Spielpfad] [--force]`). **Echt getestet** gegen das laufende Spiel: MelonLoader-Reinstall + Mod-Install (Release v0.1.0) beide erfolgreich. Lokalisiert wie der KeybindEditor. Commit `2d2d727`, lokal (noch nicht gepusht).

  10. **Ingame-Menü-Button verworfen zugunsten Startmenü-Verknüpfung**: echter Klick-Button im Hauptmenü des Spiels hätte Unity-IL2CPP-UI-Injection per Harmony gebraucht — dafür gibt's im Mod keine Vorarbeit (kein Code, der je neue UI-Elemente instanziiert, nur bestehende liest/patched) und hätte vermutlich ein Decompile des Spiels gebraucht, um die echte Menü-Button-Hierarchie zu finden. Stattdessen (Nutzerwunsch, deutlich risikoärmer): der Installer legt nach erfolgreicher Installation automatisch eine **lokalisierte Startmenü-Verknüpfung** zum KeybindEditor an (`tools/Installer/StartMenuShortcut.cs`, per COM `WScript.Shell`, kein COM-Reference nötig), in einem Unterordner benannt wie der Spiel-Installationsordner (analog dazu, wo Steam eigene Spiel-Verknüpfungen ablegen würde), Verknüpfungsname `"Disco Elysium - Accessibility Mod Tastenbelegung konfigurieren"` (DE) / `"... Configure Keybinds"` (EN). KeybindEditor nimmt jetzt optional den Spielpfad als Kommandozeilenargument entgegen (von der Verknüpfung mitgegeben), damit kein manuelles Durchsuchen nötig ist. **Echt getestet**: CLI-Install gegen das laufende Spiel ausgeführt, Verknüpfung erstellt und per Doppelklick geöffnet — KeybindEditor startet korrekt im eigenen Fenster.

  11. **Ultrareview der 4 Commits (Keybind-System + beide Tools + Startmenü-Verknüpfung) durchgeführt, alle 10 gefundenen Bugs gefixt** (Commit `46802e9`): (a) `KeyBindings.IsPressed` verlangte exakten Modifier-Match statt wie vorher toleriert zu werden — hätte z. B. `H` stumm ignoriert, wenn zufällig Shift gehalten wird; jetzt wieder toleranter, dafür `CycleForward`/`CycleBackward`-Check-Reihenfolge gefixt, damit das nicht kollidiert. (b) `Deserialize` fiel bei kaputtem Modifier-Feld nur teilweise auf Default zurück statt komplett. (c) Installer überschrieb beim Reinstall/Update `UserData/*` immer mit dem Release-Zip-Inhalt — echte Fortschritt-/Tastenbelegungsdaten wären klobbert worden (**live getestet**: simulierte Spielerdaten überstehen jetzt einen Reinstall). (d) Startmenü-Verknüpfung: Pfad mit Backslash am Ende hätte das Anführungszeichen im Kommandozeilenargument kaputt escaped. (e) CLI-Modus: Flag vor dem Pfad (`--cli --force "<pfad>"`) hat den Pfad bisher stillschweigend verworfen — **live getestet**, jetzt korrekt. (f) CLI-Modus loggte den "Verknüpfung übersprungen"-Fall nicht (GUI schon). (g) Zwei separate `HttpClient`-Instanzen im MelonLoader-Install-Schritt zu einer zusammengefasst. (h) KeybindEditor erlaubte Neubelegung auf eine bereits verwendete Taste ohne Warnung — macht die andere Aktion stumm unerreichbar; jetzt blockiert (Nutzerentscheidung per Umfrage: "Blockieren" statt "nur warnen"). (i) Escape war hart als Abbrechen-Taste in der Aufnahme codiert und konnte dadurch nie selbst belegt werden, obwohl es eine reale Preset-Belegung ist — dediziertem "Neubelegung abbrechen"-Button hinzugefügt, Escape ist jetzt normal belegbar. Nebenbefund (kein Bug unsererseits): die "persönlich" wirkenden Wegpunktdaten in `mod/Data/AccessibilityMod_Waypoints.cfg` stammen vom Upstream-Maintainer selbst (Commit von LordLuceus, bereits im echten Upstream-Repo `game-a11y/Disco-A11y`) — bewusste "Community-Wegpunkte"-Beispieldaten, kein Datenleck.

  12. **Spiel decompiliert** (`ilspycmd` auf `MelonLoader/Il2CppAssemblies/Assembly-CSharp.dll` → `disco/`-Ordner, der in der `.gitignore` des Repos genau dafür schon vorgesehen war; 1937 Stub-Dateien — nur Signaturen/Namen, keine Methodenkörper, da IL2CPP). Ziel: Lücken finden, die die Mod noch nicht abdeckt. **Funde:**
      - Das Spiel hat einen **eigenen `Accessibility`-Namespace** (ZA/UM-Code, nicht Unity): `AudioCaptionsManager` (Untertitel für Soundeffekte, primär für Gehörlose; Hook: `OnSoundPlayed(group, variation)` / `ShowCaptions(audioName)`), `AccessibilityTests` (Settings-Toggles: `ToggleUseCaptions`, `ToggleMonoSound`), `InputDelayController`, `AccessibilityManager.isAccessibilityEnabled` (wird nirgends im Managed-Code abgefragt, vermutlich rudimentär/Konsole). **Kein** eigener Screenreader/TTS — `VoiceOverSystem` ist nur das Final-Cut-Voice-Acting.
      - **`GameEntity.CheckIfCanCreatePathToHavePath(Location)`** (Il2CppFortressOccident) — eingebauter Erreichbarkeitstest inkl. `_navPath`/`IsWithinInteractionRadius`. Damit könnte die Mod schon bei der Auswahl "nicht erreichbar" ansagen statt erst nach fehlgeschlagenem Autowalk ("Movement failed").
      - `MouseOverHighlight.registry` ist bestätigt die **einzige** Interaktions-Objektquelle des Spiels (auch `CommonPadInteractable`/Controller-Pfad baut nur darauf); Objekte ohne diese Komponente bleiben strukturell unsichtbar → Waypoints bleiben die richtige Antwort dafür.
      - Karten-Ansicht der weißen Checks (`SkillcheckListPage`/`MapSkillcheckDetailPage` in Il2CppPages.Gameplay.Map) vermutlich ungepatcht (Journal-Variante ist gepatcht).
      - Hilfe-Overlays (`HelpOverlayManager`/`GameHelp`/`PauseHelp`) ungepatcht.
      - Spiel-eigenes Rebinding-System (`Il2CppRebind`: RebindingUI/RebindActionRecord) — Chance: KeybindEditor könnte Konflikte zwischen Mod-Tasten und Spiel-Tasten erkennen.
      - Positivbefund: Quicktravel, Journal (Tasks + weiße Checks), Notifications, Confirmations, Floats, Inventar sind bereits gepatcht — die Mod ist deutlich reifer als die README vermuten lässt.

  13. **Alle vier Decompile-Funde umgesetzt** (Nutzer wollte alle; Code fertig + gebaut, Commit ausstehend wegen Session-Limit/Klassifikator): (a) Sound-Untertitel → TTS (`mod/Patches/AudioCaptionPatches.cs`, Postfix auf `AudioCaptionsElement.ShowCaption`, neues Setting `SpeakAudioCaptions` inkl. KeybindEditor-Checkbox, erzwingt `UseCaptions=true` beim Szenenladen). (b) Erreichbarkeits-Ansage (`mod/Utils/ReachabilityChecker.cs` via Unity `NavMesh.CalculatePath`, `UnityEngine.AIModule`-Referenz ergänzt; `NavigationInfo.IsReachable` → "Not reachable on foot from here." in der Objektansage; nur definitives false wird angesagt). (c) Karten-Skillcheck-Liste: **kein Code nötig** — Liste UND Detailseite nutzen dasselbe bereits gepatchte `PageSystemJournalWhiteCheckUI`. (d) Hilfe-Overlay (`mod/Patches/HelpOverlayPatches.cs`, liest beim Öffnen alle TMP-Texte des PauseHelp-Panels vor) + **Spiel-Keybind-Konflikt-Check** (`mod/Settings/GameKeybindConflictChecker.cs`, liest zur Laufzeit die echten InControl-Bindings inkl. Ingame-Rebinds über `CrossPlatformInputManager.InputActions`, dumpt alles ins Log, sagt kurze Zusammenfassung an; Namens-Aliasse Keypad→pad, Alpha→num nötig gewesen).
      **Live getestet** (2 Spielstarts): Konflikt-Check fand 11 echte Kollisionen, darunter neue Erkenntnisse über UNSERE Presets: `Escape` (StopMovement, NumpadSafe) öffnet gleichzeitig das Pausenmenü; `KeypadMinus` (Dialog-Lesen-Toggle) zoomt gleichzeitig raus; `Keypad1-3/6-8` wählen im Dialog Dialogoptionen (DialogueShortKeys — Risiko ungewollter Story-Entscheidungen beim Szene-Scannen im Dialog!); `Alpha8`/`H`/`R` sind Upstream-Altlasten (DialogueShortKey8/PhotoMode/Collage-Redo). Freie Ausweichtasten laut Dump: F2-F4, F6-F8, F10-F12, `KeypadDivide`, `KeypadMultiply`; `Space` = spieleigenes StopMovement (wäre konsistent).

  14. **Release v0.2.0 veröffentlicht** (Commit `74e9ea9` gepusht, `gh release create v0.2.0` auf dem Fork, Zip nach v0.1.0-Muster: Mods/AccessibilityMod.dll + Tolk-DLLs + UserData/Waypoints; Release-Notes mit vollständigem Changelog, sauber ohne projektfremde Erwähnung). Beide Tools neu gepublisht (KeybindEditor mit neuer Geräusch-Untertitel-Checkbox), Editor liegt neben dem Installer. **Installer end-to-end gegen v0.2.0 getestet**: lädt/installiert das neue Release korrekt (DLL-Hash identisch mit frischem Build), Nutzerdaten (Waypoints + Keybind-Config) blieben dank Skip-if-exists unangetastet, Startmenü-Verknüpfung erstellt.

  15. **Parser-Bug gefixt + Tools erstmals als Release-Asset veröffentlicht**: (a) KeybindEditor-Parser versteht jetzt auch MelonPreferences' TOML-Inline-Tabellenformat (`AccessibilityMod = { ... }` in einer Zeile — so stand es real in der Config nach Spielende); verifiziert per Test-Harness gegen die echte Datei + synthetische Sektions-/Inline-Fixtures mit Nicht-Default-Werten (Commit `8b0c526`). (b) Kein neuer Release-Tag nötig (Fix betrifft nur das Tool, Mod-Zip unverändert) — stattdessen `DiscoElysiumTools-0.2.0.zip` (Installer + KeybindEditor + Tolk-DLLs) als zweites Asset ans bestehende v0.2.0-Release angehängt, Release-Notes um Asset-Beschreibung ergänzt. (c) Dabei Stolperfalle behoben: Installer nahm blind das erste Zip-Asset — bevorzugt jetzt `DiscoElysiumAccessibilityMod-*.zip` per Name (Commit `b16f250`, gepusht). **Regressionstest bestanden**: CLI-Install gegen das Release mit zwei Zips lädt weiterhin korrekt das Mod-Paket.

  16. **Presets nach Nutzerentscheidung umgebaut + Release v0.3.0** (Commit `612342f`, gepusht, Release mit Mod- und Tools-Zip live): Nutzer entschied per Umfrage: Leertaste für Bewegung-Stoppen (spielkonsistent), Ziffernblock-Geteilt für Dialog-Lesen im Numpad-Modus, F-Tasten für die Kategorien — plus **neue Regel: es gibt jetzt ZWEI layoutsichere Presets, der Stardew-ähnliche Modus muss komplett ohne Ziffernblock auskommen.** Umsetzung: beide Presets teilen F2/F3/F4 (Kategorien; Wegpunkte als Strg/Alt-Varianten davon), F6/F7/F8 (Ansage/Sortierung/Scan), Leertaste (Stopp), PageUp/PageDown/Strg+Home (stardew-Konventionen), F11 (Sprachunterbrechung — Alpha8 wählte im Dialog Option 8!), Strg+B (Diagnose); `NumpadSafePreset` nutzt zusätzlich Keypad0 (Alles) + KeypadDivide (Dialog-Lesen) — die einzigen kollisionsfreien Numpad-Tasten —, `StardewPreset` (neu) stattdessen F10 + F12. Wichtig wegen Toleranz-Matching: jede Aktion außerhalb der Kategorie-Kette braucht eine eindeutige Basistaste (deshalb kein geteiltes F12 für Dialog+Diagnose o. ä.). KeybindEditor: dritter Preset-Button ("Mit/Ohne Ziffernblock"), `ActionInfo` um `StardewBinding` erweitert, CLAUDE.md-Doku aktualisiert. Bestehende Nutzer-Configs bleiben unberührt (Presets muss man aktiv laden — steht so auch in den Release-Notes).

  17. **Dialog-Auto-Advance ("Autoread") gebaut + Release v0.4.0** (Commit `44c9b15`, gepusht, Release live): Neuer Toggle auf **G** (in allen drei Presets, remappbar als `GameKey.ToggleDialogAutoAdvance`, persistiert als `DialogAutoAdvance`, Checkbox im KeybindEditor). Sobald der Screenreader die aktuelle Dialogzeile fertig gesprochen hat (`Tolk.IsSpeaking()`-Polling), drückt die Mod den spieleigenen Weiter-Button (`SunshineContinueButton.instance.buttonComponent.onClick`, per Decompile gefunden) — Gespräche laufen wie ein Hörbuch durch. Schutzmechanismen: nur im Volltext-Lesemodus (sonst würde Ungelesenes übersprungen), max. 1× pro gerenderter Zeile (`DialogSystemPatches.LineCounter` — ohne Screenreader kann nichts durchrasen), 0,6 s Nachlauf nach Sprachende + 1 s Mindestabstand, und der Button-Zustand des Spiels entscheidet (bei Antwortoptionen/Checks ist er disabled/versteckt → pausiert automatisch, Antworten wählt man normal selbst). Smoke-Test: Spiel startet fehlerfrei, neue G-Belegung landet in der Config.

  18. **Interaktionstaste F + dynamische Tasten-Hinweise** (Commit `b9d8adf`, gepusht; Live-Diagnose mit dem spielenden Nutzer): (a) Nutzer stand vor einem Objekt ("Shoe Snakeskin Left"), drückte E — nichts passierte. Log+Decompile-Analyse: **Das E-Interact des Spiels wirkt nur auf die Controller-Auswahl** (`InteractableSelectionManager`), die bei reinem Tastaturspiel nie befüllt wird; Interaktion war faktisch Mausklick-only. Neue remappbare Aktion `InteractWithSelected` (**F** in allen Presets, im Spiel frei) klickt das Mod-ausgewählte Objekt direkt über `MouseOverHighlight.InteractFirstActive(ClickEventData)` an, mit "zu weit weg"-Hinweis über den `GameEntity.IsWithinInteractionRadius` des Spiels. (b) Nutzer-Feedback: Hinweistexte ("press period to cycle, comma to navigate", "Alt plus Left Bracket") waren hartcodierte Upstream-Tastennamen — jetzt nennen alle Ansagen die **live gebundene** Taste (`KeyBindings.SpeakableName`, macht aus "Ctrl+PageDown" sprechbares "Ctrl plus Page Down"). (c) Nebenbei gelöst: Strg+Pos1 ging nicht, weil Nutzer die Ziffernblock-7 als Pos1 nutzte — Unity ignoriert NumLock und sieht `Keypad7`, nicht `Home`; mit dedizierter Pos1-Taste funktioniert es.

  19. **Kategorie-Zyklus (stardew-Stil) + Editor-CLI für Config-Updates** (Commit `7aebd27`, gepusht): (a) Nutzerwunsch: **Strg+Bild↓/Bild↑ = nächste/vorherige Kategorie** wie in stardew-access (dort verifiziert: "left ctrl + pageUp/pageDown = previous/next category") — durchläuft NPCs→Orte→Loot→Alles mit Umbruch, im Waypoint-Fokus stattdessen die Waypoint-Filter (alle→NPCs→Orte). Wegen des toleranten Modifier-Matchings werden die Strg-Varianten in der Dispatch-Kette VOR den einfachen Bild-Tasten geprüft. (b) Nutzerwunsch "Auto-Update für den Tasten-Editor": **`DiscoElysiumKeybindEditor.exe --cli [Spielpfad] [--preset default|numpad|stardew] [--list]`** — Standardaktion "Sync" ergänzt nur die seit dem letzten Speichern neu hinzugekommenen Aktionen mit Default-Belegung (bestehende Belegungen/Einstellungen bleiben unangetastet) und benennt sie im Output; `--preset` ersetzt alle Belegungen; `AttachConsole` macht den Output im aufrufenden Terminal sichtbar. **Zweimal live gegen die echte Config getestet**: erst wurde genau `InteractWithSelected` ergänzt, nach dem Kategorie-Feature genau `CycleCategoryForward/Backward` — Nutzer hatte zwischenzeitlich selbst das "Ohne Ziffernblock"-Preset geladen, blieb korrekt erhalten.

  28. **Update-Infrastruktur komplett** (Commit `62f7619`, gepusht, Nightly-Assets inkl. `installer-version.txt` + `mod-version.txt`; Anlass: Freundin des Nutzers scheiterte an Nightly-Upgrade mit alter Binary): (a) **Pflicht-Self-Update des Installers**: BuildId (AssemblyMetadata, beide Flavors via `-p:InstallerBuildId` synchron, Target schreibt `installer-version.txt` beim Publish) gegen Nightly-Asset; bei Abweichung lädt er sein Flavor-Asset, ersetzt sich **in-place** (Rename auf .old, neue Exe an Originalpfad, Neustart mit Original-Argumenten, .old-Cleanup) — ohne erfolgreichen Check keine Installation (Gate, 404 live getestet); Dev-Escape `--no-selfupdate`/BuildId "dev". **E2E live getestet**: alte Exe erkannte neue Version, ersetzte sich, Zweitlauf up-to-date. (b) **.NET-6-Check** (Mod-Laufzeit-Voraussetzung): GUI fragt + installiert offiziellen MS-Installer silent, CLI automatisch. (c) **Preset bei Erstinstallation**: GUI fragt "Ziffernblock vorhanden?", wendet numpad/stardew via Konfigurator-CLI an; CLI `--preset` (Default numpad) — vorher bekamen Neuinstallationen die kaputten US-Defaults! (d) **Mod sagt Version an** ("… Mod nightly-20260712194740 loaded", auch im Log; `ModVersion`-AssemblyMetadata via `-p:ModVersion` — Stolperfalle: `GenerateAssemblyInfo=false` verwarf die Metadata still, erst "build dev" angesagt). (e) **Mod-Update-Hinweis beim Start** (Hintergrund-Check, stable→latest-Tag, nightly→mod-version.txt, lokalisiert: "Zum Aktualisieren den Installer ausführen"). (f) **Überschreib-Dialog** "Installierter Mod vX. Mit vY überschreiben?" (Versionsmarker `Mods/AccessibilityMod.version.txt`; GUI fragt, CLI loggt). (g) **Live gefundener Bug gefixt**: `--prerelease` installierte v0.2.0 statt Nightly (in-place-Nightly behält altes created_at, GitHub-Liste sortiert danach) — Kanal bevorzugt jetzt explizit den nightly-Tag. **Merke fürs Packaging**: Releases immer über die BuildId-Pipeline bauen (`-p:InstallerBuildId=X -p:ModVersion=Y`, installer-version.txt+mod-version.txt mit hochladen), sonst blockt das Gate bzw. lügt die Ansage. Stick war nicht angeschlossen — bei nächster Gelegenheit `DiscoElysiumSetup.exe` (Standalone, BuildId 20260712194740) neu drauf.

  29. **Dialog-Dauersperre gefixt** (Commit `a500ee4`, gepusht, Nightly komplett refreshed auf Build `20260712204504`; Bug bei Nutzer UND Freundin verifiziert: "Wir kommen aus einem dialog nicht raus"): Das Signal aus Punkt 27a (`SunshineContinueButton.activeInHierarchy`) bleibt nach Dialogende dauerhaft true → Navigations-Sperre für immer aktiv. Jetzt stattdessen das globale semantische Signal des Dialogsystems selbst: `Il2CppPixelCrushers.DialogueSystem.DialogueManager.IsConversationActive` (statisch, per Decompile von Il2CppDialogueSystem.dll gefunden) — zuverlässig in beide Richtungen, im Einklang mit der Global-statt-Szenen-Regel. **Voller Zyklus live per DevBridge validiert**: false im Menü und nach Save-Load, true während des Badezimmerspiegel-Dialogs, false nach Verlassen des Dialogs. DevBridge-`state` meldet dazu jetzt `dialogUiActive`. Nebenerkenntnis: verschlossene Türen lösen KEINEN Dialog aus (interact feuert dort ins Leere — kein Panel, kein Dialog); der Spiegel ist ein verlässlicher Dialog-Trigger zum Testen. **Offen**: Stick war nicht angeschlossen — `DiscoElysiumSetup-standalone.exe` (Build 20260712204504) bei nächster Gelegenheit auf den Stick; Nutzer-Frage nach Socket-Transport für die DevBridge statt 5-Hz-Datei-Polling (Antwort gegeben: Polling kostet praktisch nichts, Socket brächte Push-Events — ggf. als DevBridge v2 bauen, auch als Blaupause fürs ein Regelwerk); Container-Panel ("Alles nehmen/Schließen") vermutlich unvertont.

  30. **Container vertont, verschlossene Türen angesagt, DevBridge mit Socket** (Commits `ad2da54` + `eab03d9`, gepusht, Nightly `20260713012055`): (a) **Welt-Container waren komplett stumm** (Nutzer wusste nicht mal, dass es sie gibt) — `Sunshine.Container` ist ein **Singleton**, durch das JEDER lootbare Behälter im Spiel läuft, also global gepatcht (SetItems→Inhalt ansagen, OnDisable→"Container geschlossen"); Interakt-Taste bedient bei offenem Panel das Panel ("alles nehmen"). Item-Keys werden über `InventoryItemList.singleton.GetByName()` zum **lokalisierten Anzeigenamen** aufgelöst ("Leere Tonbandspule" statt "music whirling smallest church"); die "Genommen"-Ansage entfällt bewusst, da die spieleigene Notification den Namen schon sagt. Live validiert. (b) **Verschlossene Türen**: Klick verpuffte stumm → jetzt "X ist verschlossen" (via `Door._isLocked`, global). (c) **DevBridge v2**: TCP-Transport (127.0.0.1, Port in `port.txt`, Antwort endet mit `<<END>>`) neben dem Datei-Kanal — Ein-Frame-Latenz und **Push-Events** ohne Nachfragen (`! spoken <text>`, `! dialog active/inactive`, `! scene <name>`); Threads: Accept/Read im Hintergrund, Ausführung/Schreiben nur im Unity-Main-Thread. Test-Client `tools/DevBridge/bridge-client.ps1`. (d) **UpdateNotifier-Bug**: meldete jede *abweichende* Version als Update — also auch bei neuerem lokalem Build; vergleicht jetzt Zeitstempel. (e) Neue Audit-Befehle `screen` (aller sichtbare Bildschirmtext), `names` (Namensquellen je Objekt), `modkey`/`modkeys` (Mod-Hotkeys per Bridge auslösen, via Harmony auf `KeyBindings.IsPressed` — **Release-Mod bleibt frei von Dev-Hintertüren**).
      **Gescheitert und bewusst verworfen**: Spielaktionen direkt auslösen (Nutzeridee: "dem, der auf Tastendrücke achtet, vorspielen, dass gedrückt wurde"). Beide Wege trieben das Spiel in seinen Crash-Reporter ("Ein Fehler ist aufgetreten", per Screenshot verifiziert): `PlayerAction.UpdateWithState/Commit` schreibt in InControls Zustand; Harmony auf `OneAxisInputControl.get_WasPressed` ist eine **virtuelle** Methode, die das Spiel jeden Frame für jede Aktion liest (Harmony warnt explizit). Spielmenüs laufen daher über `key` = echter OS-Tastendruck, der den Fensterfokus zieht (nur für unbeaufsichtigte Sessions — hat dem Nutzer live ein Tab auf den Desktop gedrückt).
      **Release-Kanäle geklärt** (Nutzerfrage "wir brauchen zwei Kanäle" + "Experimentelles bleibt draußen"): Es gibt sie bereits — `nightly` (Pre-Release, Installer-Haken, in-place, alles Neue) und `v0.2.0` (Latest, Installer-Standard, stabil). **Wichtige Korrektur einer Fehlannahme**: v0.2.0 zeigt auf `4bc6041` und enthält die Dialog-Sperre GAR NICHT (kam erst mit `698b24f`, nur Nightly) — der stabile Kanal hat den Dauersperre-Bug also nie gehabt, betroffen waren nur Nightly-Nutzer. Ein v0.2.1-Bugfix-Release wurde daher NICHT gemacht (wäre Release-Spam ohne Blocker). **Regel fürs nächste stabile Release**: erst nach echtem Durchspielen, und nur Getestetes — Experimentelles/Neues bleibt bis dahin im Nightly.
  31. **Objektnamen untersucht → es gibt keine besseren; Unity-Name bleibt** (Nutzerfrage nach den "echten" Namen; zwei Live-Messungen per neuem `names`-Bridge-Befehl): (a) **Conversant-Actor der Conversation liefert FALSCHE Namen**: "Kims Paperwork" → **"Cuno"**, "Tequilas bed messy" → "Bed" (der Gesprächspartner ist eben nicht das Objekt). (b) **Sprecher der ersten Dialogzeile**: durchgehend leer; von 8 Objekten mit Dialog kennt die Master-DB überhaupt nur 3 (Conversations werden dynamisch nachgeladen). (c) **`Localization.Language` ist leer** → Actor-Namen sind englisch ("Mirror"); das deutsche "Spiegel:" im Gespräch entsteht erst beim **Rendern der Dialogzeile**, hängt nicht am Objekt. (d) **Das Spiel zeigt Sehenden gar keine Objektlabels** (Tab blendet nur Orbs ein, keinen Text) — es existiert schlicht kein Anzeigename in den Spieldaten. **Fazit**: Der aufgehübschte Unity-Name (Level-Designer-Name) ist die einzige und beste Quelle — Upstream hat das richtig gemacht. Der Umbau wurde nach dem Test wieder entfernt; Befund als Kommentar in `ObjectNameCleaner.GetBetterObjectName` festgehalten, damit es niemand erneut "verbessert". Nutzer-Einwand "nicht jedes Objekt kann sprechen" war goldrichtig und hat den Fehler früh aufgedeckt.

  32. **Echte Item-Namen + Screen-Ansagen** (Commit `608ed07`, gepusht; **Nutzeridee**: "wie die items, zumindest die, die wir aufnehmen können, heißen, wenn schon türen nicht funktionieren"): (a) **Aufnehmbare Objekte heißen jetzt richtig**: Loot-Objekte sind ContainerSources mit **genau einem** Item, dessen echten, lokalisierten Namen die Item-DB kennt → `empty_beer` = **"Glastara"**, `Dress Shirt Hanging` = **"Weißes Satinhemd"**, `Disco-ass blazer` = **"Ultimativer Discoblazer"**, `Piles of tape` = **"Leere Tonbandspule"**. Live validiert ("Loot: Leere Tonbandspule 4 meters south"). Container mit MEHREREN Items behalten ihren Namen (sind Kisten, keine Items — Inhalt kommt beim Öffnen). Türen/Möbel behalten den Unity-Namen (haben keine Items; für sie existiert nachweislich kein Anzeigename, siehe Punkt 31). Global über MouseOverHighlight→ContainerSource, kein Objekt-Sonderfall, mit Instance-ID-Cache (pro Szene geleert). (b) **ScreenAnnouncer** (neu): sagt beim Öffnen an, **welcher Screen** (lokalisiert) und **die Zahlen, die es nur auf Screen-Ebene gibt** — Fähigkeitspunkte (!), Erfahrung, Level, Anzahl Gegenstände. Vorher las der Charakterbogen nur den fokussierten Skill vor, ohne je zu sagen, dass man im Charakterbogen ist oder **Punkte zu vergeben hat**. Quelle: `ViewController.GetCurrentView()` + `ViewType`-Enum (die eigene Screen-Logik des Spiels) und `PlayerCharacter` (SkillPoints/XpAmount/Level) — global, kein UI-Scraping. Nur Screens, die der Spieler bewusst öffnet (sonst plapperte er interne Views wie CLEAR/SPECIAL). (c) **Nebenbefund**: Das `DiscoPages`-Page-System (36 Klassen) ist auf dem PC **toter Code** — `FindObjectsOfType<DiscoPage>` liefert 0 auch bei offenem Charakterbogen; die PC-Version nutzt `Sunshine.Views`. (d) **Komplettes Decompile** (Nutzerwunsch): alle 117 Il2Cpp-Assemblies nach `disco-all/` (68 MB, git-ignored) — kein Nachladen einzelner Assemblies mehr.

      **Nightly `20260713022400` hochgeladen** (Nachtschicht 13.07. ~03:00: Mod-Zip, Tools-Zip, beide Setup-Exes, mod-version.txt, installer-version.txt, Release-Notes). **Token-Selbstauskunft eingerichtet**: Statusline-Skript (`~/.claude/statusline-usage.py`, Python — jq fehlt/choco bräuchte Admin; Stolperfalle: `%USERPROFILE%` wird von der Statusline-Shell NICHT expandiert, absoluter Pfad nötig) schreibt `rate_limits` (5h/7d used_percentage + resets_at) nach `~/.claude/usage.json`; Helfer `python ~/.claude/usage.py`. Damit exakter Reset-Zeitpunkt lesbar → genau EIN Nachtschicht-Wecker auf resets_at+1min (Nutzer-Idee) statt stündlicher Blindtimer.

      **Nachtschicht-Validierung (13.07. ~03:00–03:15, Fensterübernahme MIT TTS-Protokoll — hat funktioniert)**: Screen-Ansagen live bestätigt: Charakterbogen → **"Charakterbogen. Erfahrung 5, Level 1."**, Gedankenkabinett → "Gedankenkabinett" (beide per HUD-Klick, ohne Fokus). **Karte nicht prüfbar**: kein MAP-Eintrag in der ViewType-Enum (Karte ist kein View, vermutlich Quicktravel-System — dessen Buttons die Mod schon patcht); M/J/I werden im Tag-1-Spielstand vom Spiel ignoriert (Tutorial-Sperre, kein Emulationsfehler — Escape kam nachweislich an). Journal/Inventar-Ansagen laufen über dieselbe ViewType-Logik und feuern beim echten Spielen.

      **Karte geklärt — es gibt auf dem PC keine** (Nutzeridee "kannst du nicht einfach ein Level über Debug laden?" führte hin): `MapPage` liegt im `DiscoPages`-System (auf dem PC toter Code, siehe Punkt 32c), `MapManager` gehört zum Collage-Fotomodus. Das PC-Äquivalent der Karte sind die **Quicktravel-Marker** in der Welt — und die deckt die Mod längst ab (`mod/Patches/MapPatches.cs`: OnSelect/OnPointerEnter/Verfügbarkeit/Reichweite). Keine Lücke, nur ein irreführender Name. (**USB-Stick erledigt sich**: seit dem Pflicht-Self-Update des Installers holt sich jede alte Exe den aktuellen Stand selbst — keine Stick-Kopien mehr nötig, Nutzerentscheidung 13.07.)

  40. **Fünf Spielsession-Bugs gefixt + die Doubletten waren ein Messfehler des eigenen Werkzeugs** (Commits `f584049`, `96a4c20`, weitere folgen): (a) **#36 Beschreibung beim Laden**: Der erste Bereich nach einem Spielstand-Laden blieb komplett stumm — die Regel "die erste Szene kennt der Spieler ja" unterdrückte nicht nur den *Namen*, sondern auch die *Beschreibung*. Genau beim Aufwachen nach dem Laden schaut ein sehender Spieler sich aber um. Jetzt: Name still, Beschreibung immer. (b) **#38 Erstbesuch-Einführung** (Nutzer: *"ich wusste nicht, dass ich in einem Hostel aufwache"*): lange Einordnung beim allerersten Betreten eines Bereichs (`AreaDescriptions.Intros`, persistiert in `SeenAreaIntros`), jederzeit per **Strg+U** abrufbar. Gesprochen wird sie nicht beim Szenenladen, sondern **sobald der Spieler zum ersten Mal die Kontrolle hat** (Dialog vorbei) — global, kein Szenen-Sonderfall. (c) **#37 Klaasje-Phantom** (Nutzer: *"warum sehe ich das Objekt, kann aber nicht hinlaufen?"*): Die `MouseOverHighlight.registry` enthält **Platzhalter für abwesende Personen am Weltursprung (0,0,0)** — dort scheitert das NavMesh-Sample, also galten sie als "erreichbar unbekannt" und blieben in der Liste. Neuer Filter `GameObjectUtils.IsActuallyInWorld` (Position != Vector3.zero). (d) **#32 Liste veraltet nach Türöffnung**: Die Objektliste war ein Schnappschuss vom Moment des Kategorie-Tastendrucks — Tür auf, durchgegangen, und die Liste bot weiter den alten Raum an. `NavigationStateManager.RefreshIfStale` baut sie beim Blättern neu auf, wenn sie älter als 4 s ist, und hält dabei die Auswahl fest (Instanz-ID). (e) **#30 Leere Container**: Die ausgeräumte Tonbandspule meldete sich weiter als Beute ("1 of 1") — Ursache war ein **Namens-Cache**, der das Leeren überlebte. Cache raus (live auflösen), plus `ObjectNameCleaner.HasLootableContents`: ein Container ohne Inhalt ist keine Beute mehr (bleibt unter "Alles", wo ihn auch ein sehender Spieler noch liegen sieht).
      **(f) Die vermeintlichen Doppel-Ansagen gab es nie — mein Messgerät war kaputt.** Ich habe stundenlang einen Dedup-Bug in den Orb-Ansagen gejagt, weil der Bridge-Mitschnitt jede Zeile zweimal zeigte (~400 ms versetzt, alternierend). Im echten Sprechprotokoll steht **jede Zeile genau einmal** — der Spieler hat nie etwas doppelt gehört. Ursache: Die Bridge hing per Harmony-Postfix an `TolkScreenReader.Speak`, und eine **queued** Ansage läuft da **zweimal** durch (einmal beim Einreihen, einmal beim Abspielen). Der alte Workaround verglich nur gegen die *unmittelbar vorige* Zeile und versagte deshalb bei zwei abwechselnden Sprechern (A, B, A, B). Richtig gelöst statt geflickt: die Mod hat jetzt ein **`TolkScreenReader.Spoken`-Ereignis**, das genau einmal pro Zeile im Sprech-Moment feuert (dieselbe Stelle, an der auch das Protokoll schreibt) — Bridge, Sprechprotokoll und der geplante Mod-Debugger (#35) teilen sich damit **eine Quelle der Wahrheit**. Lehre: Wenn Werkzeug und Protokoll sich widersprechen, hat das Protokoll recht — es hängt näher am Ohr des Nutzers.
      **(g) Alter Fehler wiederholt**: Das Spiel per `Start-Process` **ohne `-WorkingDirectory`** gestartet → MelonLoader las `UserData/` relativ zum falschen Ordner und lud **Default-Einstellungen statt der Nutzer-Config** (Dialog "Disabled" statt "Full", kein Debug-Log). Steht seit Punkt 7(b) in dieser Liste; als Memory gespeichert, damit es nicht ein drittes Mal passiert.

  55. **Windows-Update KB5101650 macht MelonLoader stumm kaputt → „Disco-Doktor" gebaut** (17.07., live diagnostiziert und repariert; Commit + Nightly-Refresh siehe unten): (a) **Diagnose**: Nach dem Juli-Windows-Update startete das Spiel ohne MelonLoader — `version.dll` lag unverändert im Spielordner, aber der Loader zog `C:\Windows\System32\version.dll` vor (per Modul-Liste des laufenden Prozesses verifiziert; kein `Latest.log` mehr geschrieben; `version.dll` ist NICHT in den KnownDLLs, weder Registry noch `\KnownDlls`-Objektverzeichnis per `NtOpenSection` — exakter Loader-Mechanismus offen). **Fix verifiziert**: Proxy in `winmm.dll` umbenannt → Bootstrap lädt normal, Mod spricht. ~~Betrifft JEDE MelonLoader-Installation nach dem Update~~ **Korrektur nach Nachtforschung (18.07., Nutzer-Beobachtung „die anderen Games funktionieren noch" war der Schlüssel)**: Das Update ändert nur die **dynamische** Auflösung (`LoadLibrary`/Delay-Load) von `version.dll` → System32; **statische Importe** lösen weiter App-Dir-first auf. Beweis: Sonden-Exe im Spielordner (`LoadLibraryExW("version.dll")` → System32 in JEDEM Ordner, `winmm.dll` → lokal) vs. laufendes DDLC (Unity 2019, statischer Import), das seine lokale `version.dll` nach dem Update normal lud. Disco (Unity 2020.3) lädt sie dynamisch → nur dort brach es. Freundin betrifft es also nur bei Disco — und das heilt Installer/Doktor. (b) **Modularer Fix-Katalog** `tools/Shared/ModFixes.cs` (`ModFixCatalog`, per `<Compile Include>` in Installer UND Konfigurator; künftige Fixes = eine Subklasse + Katalogeintrag + `FixName_<Id>`-String in beiden `Strings.cs`): Installer wendet ihn bei **jeder** Installation direkt nach dem MelonLoader-Schritt an (präventiv: frisch entpackte `version.dll` wird sofort umbenannt; `IsInstalled` akzeptiert beide Proxy-Namen). (c) **„Disco-Doktor"** (Nutzer-Name): Reparatur-Fenster im Konfigurator (Button im Hauptfenster; einspaltige Problem-Liste mit Zustand, „Jetzt reparieren", Tolk-Ansagen, verweigert bei laufendem Spiel) + CLI `--repair`. **Getestet**: alle drei CLI-Pfade gegen Fake-Ordner (Umbenennen / nichts zu tun / beide Proxies → `version.dll.disabled`) + echter Spielordner (korrekt „OK"); GUI-Fenster noch nicht vom Nutzer getestet → bleibt Nightly. **Offen**: `winmm.dll` könnte ein späteres Update genauso treffen — dann neuen Proxy-Namen als weiteren Fix in den Katalog. (d) Commit `646a45b`, gepusht. **Stabiles Release v0.5.0 veröffentlicht** (Nutzerentscheidung: „sehr stabiles System", direkt ohne GUI-Test des Doktor-Fensters; Versionswahl v0.5.0 statt 1.0.0 per Umfrage): Mod-DLL quellidentisch mit dem wochenlang bespielten Nightly-Stand, nur mit `-p:ModVersion=v0.5.0` neu gestempelt; Zips aus den Nightly-Zips abgeleitet (nur geänderte Dateien getauscht); Setup-Exe/BuildId `20260717235603`; Nightly-Assets (Setup, Tools-Zip, installer-version.txt) in-place nachgezogen, da das Self-Update-Gate immer aufs Nightly zeigt; `/releases/latest` liefert verifiziert v0.5.0, Gate-Datei verifiziert. Mod-Nightly-Zip + mod-version.txt bewusst unangetastet (Mod-Code unverändert).

  57. **Volles Multi-Agenten-Code-Review über Janas PR #1 gefahren — 10 verifizierte Findings, Fixes an Jana übergeben** (18.07., Kommentar: PR #1 issuecomment-5011842892): 8 parallele Finder (Zeile-für-Zeile, entferntes Verhalten, Cross-File, Reuse, Simplification, Efficiency, Altitude, Conventions), Verifikation im Hauptkontext (Token-Budget!). Top-Findings: (1) **Strg+1/2 im Dialog wählt still Dialogoption 1/2** (Spiel liest Ziffern trotz Strg — Story-Risiko); (2) Interakt-Taste feuert bei offenem Splash erst Welt-Interaktion, dann Schließen (Dispatch-Reihenfolge); (3) globale Sprachunterbrechung befördert die queued Hint-Ansage und **köpft das Forschungsergebnis** (`interrupt || globalInterruptEnabled`, TolkScreenReader:217); (4) Splash bei aktiver Konversation ohne Tastatur-Ausgang (Dialog-Return vor Splash-Check); dazu Dedup-Fenster, die echte Ansagen verschlucken, unerreichbarer UIElementFormatter-Pfad im Inventar, 2–4 FindObjectOfType/Frame im Kabinett-Polling, hartcodiertes Enter (CLAUDE.md-Regelverstoß) + Cleanup-Liste. Sauber verifiziert: Katalog-Sync, Preset-Kollisionen, Dispatch-Reihenfolgen, `-1`-Guard. **Kein Merge** (Review nicht leer); Verfahren komplett an Jana übergeben (Anleitung im Kommentar, inkl. Budget-Regel: Finder kosteten ~60 % eines 5h-Fensters; Lauf am Fensterende komplett verloren — 8 Agenten mittendrin gestorben). **Offen**: Janas Fixes → Danijels Gegentest J1–J3 → Merge + Nightly; Repo-Gesamt-Review ebenfalls an Jana delegiert (nach Budget).

  56. **Erster externer PR eingegangen — von Jana (`Jane0899`), PR #1** (18.07., reviewt, Kommentar gepostet, Merge bewusst zurückgestellt): Drei Spielsession-Bugs, auf ihrem eigenen Claude-Setup nach unseren Projektregeln gefixt und per DevBridge live verifiziert: (a) Inventar-Tabs mausexklusiv → Strg+Tab/Strg+Shift+Tab via `InventoryManager.Singleton`, plus Doppelansage-Dedup, Item-Zähler-Fix (zählte DiscoPages-Totcode), „leer"-Ansage; (b) Heil-Plus-Buttons maus-only → Strg+1/Strg+2 via `HealingButton.ApplyHeal` (KeyCode.Plus feuert auf QWERTZ nachweislich nie — per neuer DevBridge-Akkord-Injektion bewiesen); (c) Gedanken-Ergebnis-Splash stumm + Tastatur-Falle → Volltext-Ansage, Enter schließt, Kabinett-Slots erzählen per Polling (Harmony auf virtuelles `ThoughtSlot.OnSelect` crasht nativ — deckt sich mit Worklog #30). Review-Anmerkungen im PR-Kommentar: todos-#56-Text im PR veraltet (nennt noch Strg+Plus), Bug-Nummern kollidieren mit Worklog-Nummern, `.claude/settings.json`-Hooks (Task-Sync-Reminder) behalten wir, Frage nach möglichem Doppel-Klick beim Splash-Schließen. **Merge erst nach Danijels eigenem Gegentest** (Stable-Regel); danach Nightly-Refresh.

  54. **Natural-Stimmen-Hack recherchiert & reverse-engineered — direkter Weg ohne SAPI gefunden** (15.07., Recherche, noch kein Code; Nutzerwunsch „Pioniersarbeit, direkt nutzen"): Aufbauend auf #53. **Fund im Netz**: [`gexgd0419/NaturalVoiceSAPIAdapter`](https://github.com/gexgd0419/NaturalVoiceSAPIAdapter) (MIT) macht die lokalen Narrator-Naturals nutzbar; nach `NaturalVoiceSAPIAdapter` geklont und seziert. **Lokale Stimme ist ein AppX-Paket** (`MicrosoftWindows.Voice.de-DE.Katja.1` unter `C:\Program Files\WindowsApps\…`, ONNX-Modelle `am_v5_*`/Vocoder + `MSTTSLoc*.dat`), registriert per App-Extension `com.microsoft.voice.model.1` — die nur Narrator/Edge konsumieren (deshalb unsichtbar für WinRT/SAPI). **Der Hack ist zahm**: keine Inferenz nachbauen, sondern **Microsofts eigenes Embedded-Speech-SDK** (NuGets `Microsoft.CognitiveServices.Speech` + `.Extension.Embedded.TTS` + `.Extension.ONNX.Runtime`, gepinnt auf **1.41.1**, C#-API vorhanden) auf den Paketordner richten, mit **einem festen, aus `SpeechSynthesizerExtension.dll` extrahierten Schlüssel** `MS_TTS_KEY = "«privat – redigiert»"` als `"Key:"+KEY`. Rezept: `EmbeddedSpeechConfig.FromPath(ordner)` → Format `Riff24Khz16BitMonoPcm` → Name via `GetVoicesAsync()` → `SetSpeechSynthesisVoice(name, "Key:"+KEY)` → `SpeakTextAsync` → `AudioData` (24kHz PCM, abspielen wie gehabt). Direkt in C#/net6 in unseren Server integrierbar, **ohne** SAPI/COM/Fremd-DLL. **Grenzen**: privater Schlüssel („DO NOT use in production", kann bei Windows-Update brechen; neueste Voice-Versionen brauchen „License" statt „Key" — unsere Katja 1.0.4.0 ungetestet) → **nur persönliche Opt-in-Funktion, NIE im öffentlichen Release** (rechtlich/AV). SDK+ONNX = zig MB. **Nächster Schritt (offen, Nutzer-Go)**: Wegwerf-C#-PoC (net6, 4 NuGets @1.41.1, Katja-Ordner+Key) → spielt es Katja Natural hörbar ab? Definitiver Go/No-Go vor Integration als `EmbeddedNaturalTtsBackend` hinter `ITtsBackend`. Details/Recipe auch im Memory [[reference_windows_natural_voices_not_winrt]].

  53. **Befund: WinRT erreicht die Narrator-Natural-Stimmen NICHT — frühere Annahme korrigiert** (15.07., Diagnose, kein Code): Nutzer sah neue Stimmen nicht in der Liste und fragte, ob's an net6 vs. net10 liegt. **net6 und net10 liefern identisch** dieselben 3 OneCore-Stimmen (Stefan/Katja/Hedda) — `SpeechSynthesizer.AllVoices` fragt das OS, die Runtime ist egal. Tieferer Befund: auf dem Rechner ist **gar keine** Natural-Stimme vorhanden, aus Sicht *keiner* API — WinRT (3 OneCore), SAPI5/System.Speech (nur „Hedda Desktop", „Zira Desktop"), Registry (`Speech_OneCore\Voices\Tokens` + `Speech\Voices\Tokens`, HKLM+HKCU: kein „Natural"), Dateisystem (`Speech_OneCore` in Windows/System32/ProgramData: keine Neural-Daten). **Wichtige Korrektur der Projektannahme** (stand so in #45/#49): Die **neuronalen Narrator-Stimmen sind Narrator-exklusiv** und werden Fremd-Apps über WinRT/SAPI nicht bereitgestellt — sie würden also auch nach Installation NICHT in unserer Liste erscheinen (bekannte Einschränkung, betrifft auch NVDA). Über WinRT erreichbar sind nur die **OneCore**-Stimmen (Einstellungen → Zeit und Sprache → Sprache → Stimmen hinzufügen). **Konsequenz**: Für echte Neural-Qualität führt der Weg über ein Cloud-/Neural-Backend hinter `ITtsBackend` (ElevenLabs o. Ä.), nicht über Windows. **Offen**: Nutzer klärt, über welchen Weg er installiert hat; ggf. OneCore-Stimme installieren und per `--voices` gegenprüfen.

  52. **„Stimme testen"-Knopf im Konfigurator** (Commit `9bdf06b`, Nightly `nightly-20260715220551`; Nutzerwunsch, live „klingt gut" bestätigt): Neben dem Stimmen-Dropdown ein Knopf, der die **gewählte** Stimme bei der **gewählten** Lautstärke sofort abspielt — noch bevor gespeichert wird. **Designklärung mit dem Nutzer**: erst wollte ich es über eine Wegwerf-Temp-cfg lösen (Nutzer: „das ist ja blöd"), dann über einen Server-Modus (Nutzer: „Wiedergabe ist für den Server Wiedergabe, dem ist doch egal ob Test oder Game-Text — führ den Testmodus ein"). Umgesetzt so: der Server bekommt **`--say <volume> <rate> <voice> <text>`** — spielt **eine** Zeile mit exakt diesen Werten, ohne die cfg zu lesen. Wiedergabe ist derselbe Pfad wie eine echte Orb-Zeile (config-getriebenes `Speak()` und `--say` teilen sich `WinRtTtsBackend.SpeakDirect`), nur die Quelle der Einstellungen unterscheidet sich (argv statt cfg) — deshalb **kein** getrennter Test-Abspielpfad. Konfigurator: `VoiceTester.PlayAsync` startet den Server (Exe-Locator aus `InstalledVoices` wiederverwendet, jetzt `public FindServerExe`), sagt den Status per Tolk an, testet mit Normaltempo (100, da die UI keinen Tempo-Regler hat). **`--say` real geprüft** (Katja @ 60 %, Exit 0, hörbar). Läuft auf net6 (WinRT). Der leere Voice-Wert = Systemstandard.

  51. **Tools + TTS-Server zurück auf net6 gestellt — eine Laufzeit statt zwei** (Commit `14e542b`, Nightly `nightly-20260715213314`; kehrt #46 um): Nutzerbeobachtung — Janas Rechner schickte sie beim Setup auf die Microsoft-Seite. **Diagnose**: Das Setup ist selbst ein .NET-Programm; ohne die passende Laufzeit startet es gar nicht, und dann zeigt **Windows selbst** (nicht unser Code) den .NET-Downloaddialog — Henne-Ei, das unser eingebauter Auto-Installer nie lösen konnte, weil er erst *nach* der Laufzeit läuft. Nach der net8→net10-Umstellung (#46) brauchte die Maschine **zwei** Laufzeiten: net6 (Mod, von MelonLoader erzwungen) **und** net10 (Tools + TTS-Server). **Entscheidung des Nutzers**: alles zurück auf net6, damit es nur *eine* Laufzeit gibt — und zwar die, die der Mod ohnehin aufzwingt (der EOL-Einwand ist damit neutralisiert: net6 ist eh auf der Maschine, die Tools darauf zu setzen fügt keine neue Angriffsfläche hinzu). **Umgesetzt**: vier csproj (Installer/Konfigurator/Debugger auf `net6.0-windows`, TTS-Server auf `net6.0-windows10.0.19041.0`) — WinRT/OneCore-Stimmen laufen auf net6 einwandfrei, bei Build **und** Laufzeit verifiziert (`--voices` listet Stefan/Katja/Hedda). `DotNetRuntime.cs` installiert jetzt die **.NET-6-Desktop-Runtime** (Obermenge der Basis-Runtime) still — **eine** Installation deckt Mod + WinForms-Tools + Server. Doku/Strings/Release-Skript zurück auf net6. Tools behalten `RollForward=Major` (laufen also auch auf vorhandener Desktop-8/10). **Warum nicht choco/winget** (Nutzerfrage): beide müssten auch erst von etwas Laufendem aufgerufen werden (dasselbe Henne-Ei), winget ist nicht garantiert vorhanden (LTSC/Enterprise/Store deaktiviert/alt) und will teils Quell-Vereinbarungen bestätigt haben; der Direkt-Download (aka.ms + `/quiet`) läuft überall und eleviert per UAC selbst. **Noch offen (zurückgestellt)**: der allererste Setup-Start auf einem Rechner *ganz ohne* net6 hat weiterhin das Henne-Ei (Setup ist net6, braucht net6 zum Starten). Lösung wäre ein Vorschalt-Starter auf **.NET Framework 4.8** (ab Werk auf jedem Win10/11), der die net6-Desktop-Runtime still nachzieht und dann das Setup startet — gebaut, wenn der Bedarf real wird; jetzt entschärft, weil nur noch *eine* (die ohnehin nötige) Laufzeit fehlen kann.

  50. **Raumbeschreibungen über den TTS-Server + gemeinsame, einstellbare Wiederhol-Sperre** (Aufgabe #47, Commit `3e68a8f`, Nightly `nightly-20260715203740`; Stand: gebaut, beide Projekte kompilieren sauber, Config-Round-Trip verifiziert, in die Nightly hochgeladen — **noch nicht im Spiel getestet**): Folgeidee aus #49. (a) **Bereichsbeschreibungen und Erstbesuch-Einführungen laufen jetzt über denselben Kanal wie die Orb-Texte** (`AccessibilityMod.SpeakDescriptionOnServerOrTolk`): über den TTS-Server mit leerem Sprecher, Fallback auf Tolk, weiter als „gehört" protokolliert (`RecordExternalSpeech`). Grund identisch: lange, atmosphärische Texte gingen auf dem einen NVDA-Kanal unter, weil jede Navigations-/Eingabe-Ansage sie abschnitt; die Server-Stimme läuft parallel. (b) **Gemeinsame Wiederhol-Sperre N** (`RepeatSuppressionMinutes`, in Minuten, 0 = aus): ein einziger Regler steuert jetzt sowohl die Orb-Dauerschleifen-Drossel (vorher hart `180f` in `OrbTextVocalizationPatches`) als auch die Beschreibungen. Betritt man einen Bereich, dessen Beschreibung in den letzten N Minuten schon lief, wird sie **nicht erneut** vorgelesen — genau das „ich suche grade was und will nicht immer wieder die Raumbeschreibung"-Szenario des Nutzers. Abruf per Taste (U / Beschreibung/Intro) **spricht immer**, eine ausdrückliche Anfrage wird nie unterdrückt. Kein Abbrechen/Unterbrechen nötig (Nutzer nutzt in der Zeit NVDA weiter). (c) **Konfigurator**: neues Zahlen-Eingabefeld (NumericUpDown, screenreader-freundlich — sagt beim Pfeilen die genaue Zahl) unten in der allgemeinen Gruppe; Round-Trip per CLI gegen synthetische cfg verifiziert (Wert 7 blieb erhalten, `[Tutorial]`-Sektion und alle anderen Werte unangetastet). (d) Nebenbei zwei veraltete Doku-Stellen in der Repo-CLAUDE.md auf **net10** korrigiert (Setup-Runtime-Hinweis stand noch auf „.NET 8"). **In-Game bestätigt (15.07.)**: Stimme läuft im Spiel sauber, Wiederhol-Sperre getestet (Aufgabe #44 erledigt). Offen bleibt nur der optionale End-to-End-Test mit einer installierten **Natural-Stimme** (die OneCore-Stimmen laufen).

  49. **Orb-Stimme in einen TTS-Server ausgelagert — Zugriff auf die modernen Windows-Stimmen (OneCore + Natural)** (Stand: gebaut, isolierter Ton-Test bestanden, **noch nicht im Spiel getestet, noch nicht committed/Nightly**): Nutzerwunsch *"Stimme setzen, und vor allem auf die Stimmen der Windows-Sprachausgabe zugreifen"* — und die eigentliche Hälfte: die **Natural/Neural-Stimmen** (Narrator-Stimmen), die klassisches SAPI **prinzipiell nicht rendern** kann. Machbarkeit geprüft: die guten Stimmen (OneCore Hedda/Katja/Stefan; Natural erst nach Windows-Installation, aktuell keine da) gehen nur über **WinRT** (`Windows.Media.SpeechSynthesis`); Render in net6 **und** net10 verifiziert (Katja → WAV). **Architektur (per Umfragen mit dem Nutzer entschieden)**: Der Mod wird **dumm** — `mod/OrbSpeech.cs` schickt nur `(Sprecher, Text)` per stdin-Pipe an einen **separaten Kindprozess** `tools/TtsServer` (`DiscoElysiumTtsServer.exe`) und weiß nichts über Stimme/Engine/Lautstärke oder ob Ton ankommt. Server hält die Stimme warm, Warteschlange (Orbs überlappen nicht), liest `OrbVoice`/`OrbVolume`/`OrbRate` direkt aus der cfg (nur bei Dateiänderung neu), synthetisiert per WinRT, spielt per `MediaPlayer`, **stirbt mit dem Spiel** (Pipe-EOF). Backend hinter `ITtsBackend` gekapselt (später ElevenLabs/WAV pro Sprecher — Sprecher wird schon mitgeschickt). `SapiVoice.cs` abgelöst/gelöscht. **Tolk bleibt im Mod** (schneller, unterbrechbarer, Braille-Kritikpfad; ein Server-Absturz darf ihn nie mitreißen); Fallback auf Tolk, wenn der Server nicht startet. **Konfigurator**: Stimmen-Dropdown neben dem Lautstärkeregler, gefüllt vom Server selbst (`--voices`) statt eigener Aufzählung. **net10** statt EOL-net6 (Server läuft eigenständig); dabei alle Tools net8→net10 vereinheitlicht + Installer-Meldung auf ".NET 10 Desktop Runtime" (Aufgabe #46). Auslieferung: Server reist im **Mod-Zip** als `Mods/TtsServer/` (trägt die 25-MB-WinRT-Projektion), Installer kopiert den Ordner. **Isolierter Ton-Test bestanden**: Katja bei 60 % über den Server gehört. **In-Game bestätigt (15.07.)**: Mod→Server-Pipe läuft im echten Spiel sauber, Orb-Stimme kommt an. Offen bleibt nur der optionale Test mit einer installierten Natural-Stimme. Folgeidee als Aufgabe #47 (Raumbeschreibungen über den Server, gemeinsame konfigurierbare Wiederhol-Sperre).

  48. **Ein-Objekt-Container nehmen sich selbst + Orb-Lautstärkeregler** (Commits `67c6368`, `427e1fc`; Nightly `nightly-20260715015000`): (a) **Container mit genau einem Objekt** (Nutzer: *"macht das game etwas flüssiger"*): Bisher zwei Tastendrücke — Interagieren öffnet das Panel, Inhalt vorlesen, nochmal Interagieren zum Nehmen, „geschlossen". Bei einem einzelnen Objekt ist der Mittelschritt pure Reibung. Jetzt: sobald das Panel mit **exakt einem** Objekt aufgeht, nimmt die Mod es im **nächsten Frame** über die spieleigene „Alles nehmen"-Funktion (dieselbe wie die Interaktions-Taste am offenen Panel) — deferred, weil Nehmen die Item-Liste ändert, die das Spiel gerade in `SetItems` neu aufbaut (Re-Entry = Crash). Knapp gehalten: kurzes *„Aufgenommen."*, sonst nichts — kein „drücke F", kein „geschlossen". Der **Name wird nicht wiederholt**, weil das Spiel ohnehin *„Gegenstand/Geld gewonnen: X"* ansagt (Nutzeridee, wie im Container schon vorhanden: *"öffnen, zählen, nehmen wie wir schon nehmen"*). Mehr-Objekt- und leere Container unverändert. **Live verifiziert**: Geld-Container 16 m weg, ein Tastendruck → Figur lief hin (Garte-Fix), *„Aufgenommen."* + *„GELD GEWONNEN: +0,40 Réal"*, kein Panel, View sauber auf CLEAR. (b) **Orb-Text-Lautstärkeregler** (Nutzer: *"das nervt doch etwas"* — Orb auf SAPI war zu laut): Neue Einstellung `OrbVolume` (0–100, Standard 80), `SapiVoice` setzt sie vor jeder Zeile → Regler wirkt sofort ohne Neustart, unabhängig von der Screenreader-Lautstärke (das ist ja der Sinn der getrennten Stimme). **Konfigurator**: Schieberegler direkt neben dem Orb-Haken, mit Live-Prozentanzeige und Screenreader-Name, der den Wert beim Ziehen mitspricht. Config-Round-Trip per CLI verifiziert. **Offen für Jana**: passende Lautstärke am Regler finden (Aufgabe #44 deckt das mit ab).

  47. **Orb-Texte auf getrennte SAPI-Stimme umgestellt** (Commit `801d69d`, Nightly `nightly-20260715012500`): Nutzerbeobachtung — *"Orb-Texte sollten nicht über den Screenreader, sondern über SAPI vorgelesen werden, weil die sonst beim Screenreader untergehen, durch die anderen Sprachunterbrechungen, durch die Eingaben."* Stimmt: Die Mod schiebt alles über den **einen** NVDA-Kanal, und jede Navigations-/Eingabe-Ansage unterbricht die vorige — eine atmosphärische Sprechblase überlebte das nie. **Gelöst**: Neuer `mod/SapiVoice.cs` spricht Orb-Text über die **eingebaute Windows-SAPI-Stimme** per late-bound COM (`SAPI.SpVoice` — keine Zusatz-DLL, keine COM-Referenz, auf jedem Windows da), async (blockiert keinen Frame), **queued statt unterbrechend** (zwei Sprecher kurz nacheinander werden beide gehört). Läuft **parallel** zu NVDA statt um den einen Kanal zu konkurrieren — dieselbe Trennung wie bei der Entwickler-TTS. Fällt auf den Screenreader zurück, falls SAPI fehlt. Sprecher-Nennung, Dedup und Quellen-Drossel unverändert; nur der Ausgabekanal wechselte. **Wichtig**: Orb wird trotzdem weiter als "gehört" protokolliert (`TolkScreenReader.RecordExternalSpeech`) → Sprechprotokoll und Debugger-Transkript bleiben vollständig (der Debugger ist ja genau für solche Ambient-Bugs da). **Live verifiziert** (Tag-6-Kirche, Radio läuft): Log `[SAPI] Separate voice ready`, Orb-Zeilen kommen weiter im Bridge-Feed mit Sprechern, keine geht über den Tolk-Pfad. **Offen für Jana (Aufgabe #44)**: Klingt die SAPI-Stimme gut / liest sie Deutsch sauber (sonst Stimme setzen)? Und: Orb läuft jetzt nicht mehr über die AudioAware-Queue, die während gesprochener Dialogzeilen pausierte — beim Spielen prüfen, ob Orb störend über die Vertonung redet.

  46. **Erster Bug-Report von Jana (spielt/testet) — Garte-Interaktion gefixt + Debugger härter gemacht** (Commits `a45dfcb`, `80fb68d`; Nightly `nightly-20260715004500`): Jana hat mit dem Mod-Debugger einen Bericht kommentiert und exportiert (3575 Zeilen, kam per USB-Stick). Ihr Kommentar: *"Ich steh direkt vor dem Manager. Ich kann aber nicht mit ihm interagieren. Muss das so sein?"* — die Mod sagte ihr *"Garte is too far away, walk there first"*, obwohl sie davorstand und die Mod zwei Sekunden vorher *"Arrived at Garte"* gemeldet hatte. **Ursache**: Zwei Abstände widersprechen sich — "Arrived" heißt <2 m zum Objektursprung, der spieleigene Interaktionsradius ist enger; landet man dazwischen, ist man gleichzeitig "angekommen" und "außer Reichweite" = Sackgasse. **Fix** (`a45dfcb`): Die Interaktions-Taste **läuft bei außer-Reichweite selbst hin und interagiert bei Ankunft** (wie ein Klick beim Sehenden); sagt der Radius-Check danach immer noch nein, wird **nicht** ein zweites Mal verweigert — das Spiel bekommt das letzte Wort. **Live verifiziert**: Garte aus 4 m angesprochen, Figur lief hin, Gespräch startete. **Debugger** (`80fb68d`): Kommentare werden jetzt **sofort auf die Platte gesichert** (`UserData/ModDebugger-Notes.txt`, überleben Fenster-Schließen und Neustart) + Knopf **"Speichern unter"** (Bericht z. B. auf den Desktop/in den Mailordner). Beides in der Nightly.
      **DABEI DREI EIGENE REGELVERSTÖSSE** (vom Nutzer gerügt, als Memory verschärft): (1) GUI-Automatisierung (SendKeys) getestet, **während der Nutzer am Rechner war** — die Tasten landeten im Spiel und öffneten sein Inventar; (2) **keine Sprachausgabe** dabei; (3) **ungefragt auf den Netzwerk-Rechner** seiner Freundin (`\\jana1\`) zugegriffen, um ihre Dateien zu suchen. Regel jetzt: Bei Anwesenheit des Nutzers **keine** GUI-Automatisierung (nur Bridge, die keinen Fokus braucht) oder warten, bis er weg ist + volles Protokoll; **nie** ungefragt auf fremde Rechner — was er schickt (Stick), wird genommen, nicht selbst geholt.

  45. **Virenfehlalarm — das Setup verhielt sich wie ein Dropper** (Commit `b4b8897`, Nightly-Assets ersetzt): Defender hat `DiscoElysiumSetup.exe` auf einem fremden Rechner als **`Trojan:Win32/Bearfoos.B!ml`** in Quarantäne geschoben (Screenshot vom Nutzer). Das `!ml` verrät es: eine Maschinenlern-**Heuristik**, keine Signatur echter Schadsoftware — und lokal meldet dieselbe Exe nichts. Am Code war nichts falsch, **an der Auslieferung schon**: Die Exe war ein Single-File-Bundle, das **fremde Programme aus sich heraus auf die Platte entpackte** (Konfigurator, DevBridge), Binärdateien nachlud und startete, **sich selbst ersetzte** und neu startete — unsigniert. Das ist das Verhaltensprofil eines Droppers, und Heuristiken bewerten Verhalten, nicht Absicht. **Gefixt** (Nutzerentscheidung: Exe behalten, aber entschlacken): `tools/Installer/ToolsBundle.cs` lädt Konfigurator und Bridge **aus dem Release** (wie Mod und MelonLoader). Die Exe entpackt jetzt **nur noch Tolk** — das ist nichts, was sie installiert, sondern das, womit sie *spricht* (sonst stünde ein blinder Nutzer bis zur Netzantwort vor einem stummen Fenster). Größe: **930 KB → 500 KB**. Selbstupdate bleibt (Nutzerentscheidung). **End-to-end getestet** aus leerem Ordner: lädt Mod (inkl. Debugger), dann die Werkzeuge, legt beides ab, Verknüpfung erstellt. Release-Notiz erklärt den Fehlalarm für andere Nutzer. **Nutzerentscheidungen dazu (beide endgültig)**: (1) **keine Meldung an Microsoft** — die Ursache ist behoben, ein Report wäre nur noch Bürokratie für einen Build, den es nicht mehr gibt; (2) **keine Code-Signatur für dieses Projekt** — also nicht mehr vorschlagen. Wenn eine Heuristik künftig anschlägt, ist die Antwort: das auslösende Verhalten abstellen (wie hier), nicht ein Zertifikat kaufen.

  44. **Fehlende Bereiche: bewiesen, dass es per Bridge nicht geht** (kein Commit, reine Erkenntnis; Item-Beschreibung dabei am Originalfall validiert): Zweiter Anlauf für die fünf offenen Bereiche, diesmal **zu Fuß statt per Teleport** (die Aufgabe schlug das selbst vor). Ergebnis: **geht nicht.** (a) Der **Vorraum** lädt beim Teleport **komplett ohne Geometrie** — der Screenshot zeigt zwei Figuren im Schwarzen, sonst nichts; die Umgebung wird offenbar nur beim regulären Betreten gestreamt. (b) **Zu Fuß** kommt man auch nicht hin: Am **Tag 1** ist Klaasjes Tür verschlossen, am **Tag 6** fehlt die Treppe zum 3. Stock ganz in der Objektliste, und Interagieren mit der Tür löst keinen Übergang aus. (c) Teleport-Nebenbefund: Springt man von Szene zu Szene, behält die Figur teils die **alten Koordinaten** und steht neben dem begehbaren Boden (Autowalk meldet dann korrekt "nicht erreichbar") — `tour.ps1` mit szeneneigenen Markern umgeht das. **Konsequenz**: Die Beschreibungen entstehen, wenn der Nutzer die Bereiche im echten Spiel betritt (dann Screenshot per Bridge, während er dort steht). Nichts erfunden.
      **Nebenbei am Originalfall validiert**: In Whirling-f2 liegen die **Glastara**-Flaschen, mit denen die Frage anfing. Taste B sagt jetzt: *"Glastara. Leere Glasflasche."* Und die **Klaasje-Phantom-Filterung** ist im Live-Register nachweisbar — das Objekt "Klaasje" steht dort bei (0,0,0) und taucht in keiner Kategorie mehr auf.

  43. **Auslieferung geradegezogen — ein Weg für alles (neue Nutzer-Regel)** (Commits `a34ab0a`, `c17857f`; Nightly-Tag → `c17857f`): Ich hatte den Debugger erst als Ressource in die Installer-Exe eingebettet. Nutzer: *"warum trägt der installer den bumms jetzt mit sich? Das ist inkonsequent, er lädt ja auch sonst alles nach."* — und die richtige Antwort gleich dazu: **Die Exe gehört in das normale Mod-Zip**, sie ist Teil der Mod (wacht nur bei aktivem Debug-Modus auf). Also: keine Einbettung, kein eigener Installationsschritt, der Debugger reist im Mod-Release mit und landet mit allem anderen im Spielordner. Als **Regel** gespeichert (`feedback_one_consistent_path`) und in der Repo-CLAUDE.md verankert: *Gleiche Art Sache = gleicher Weg; jeder Seitenkanal ist etwas, das still aus dem Takt geraten kann.* Ausnahme mit Begründung: **Tolk bleibt eingebettet** — das ist nicht etwas, das der Installer installiert, sondern das, womit er *spricht*; müsste er es erst laden, wäre er für einen blinden Nutzer bis zur Netzantwort stumm.
      **Dabei zwei echte Bugs gefunden**: (a) **Der Installer konnte NICHTS installieren und trotzdem Erfolg melden.** Er suchte die Dateien, indem er in einen einzelnen Wurzelordner hinabstieg — richtig für Zips, die ihren Inhalt einpacken, katastrophal für die, die es nicht tun: Er stieg in `Mods/` hinab, fand kein `Mods/Mods/AccessibilityMod.dll`, kopierte nichts — **und schrieb trotzdem die Versionsmarke**, sodass der Spieler die alte Version fuhr und glaubte, die neue zu haben. (Genau das war mit meinem ersten Nightly-Zip von heute passiert.) Jetzt erkennt er die Wurzel an `Mods` und **weigert sich**, wenn die Mod nicht im Paket ist, statt eine Marke zu schreiben, die lügt. (b) Ich hatte beim Upload **dieselbe Exe unter beiden Namen** hochgeladen — die „standalone"-Datei war gar keine.
      **Standalone-Setup abgeschafft** (Nutzerentscheidung): Es war ein 68-MB-Download, dessen einziger Zweck es war, eine .NET-Laufzeit mitzuschleppen, die man **einmal** installiert und dann für alle Tools hat. Ein Setup-Asset bleibt; eine alte Standalone-Exe aktualisiert sich in dieses hinein und wird **vorher** darauf hingewiesen, dass sie die .NET-8-Desktop-Runtime braucht.
      **End-to-end getestet**: leerer Ordner → `DiscoElysiumSetup.exe --cli --prerelease` → lädt Nightly, kopiert Mod-DLL, Tolk, NVDA-Client **und Debugger** in den Spielordner. **Nightly-Assets**: Mod-Zip (mit Debugger), Tools-Zip (Installer/Konfigurator/DevBridge — ohne Debugger, keine Doppelquelle), Setup-Exe, beide Versionsdateien; Standalone-Asset gelöscht.
      **Nutzer-Test bestanden (15.07.)**: Mod-Debugger mit NVDA gegengetestet — Kommentare ließen sich gut setzen, der Kommentar-Dialog sagt sein Eingabefeld sauber an. Aufgabe #39 erledigt (die frühere UI-Automatisierungs-Warnung war ein Artefakt des Automatisierungswerkzeugs, kein echtes Problem).

  42. **Mod-Debugger gebaut (Strg+Y) — und er hat sofort einen Bug gefunden** (Commits `0672afc` + folgend): (a) **Neues Werkzeug `tools/ModDebugger`** (Nutzerwunsch): eigenes Fenster außerhalb des Spiels, lädt das Sprechprotokoll, hängt sich per Bridge-Socket **live** ans laufende Spiel und zeigt jede Zeile in dem Moment, in dem sie gesprochen wird; **Kommentar an jede einzelne Zeile** (Enter/F2) und **Bericht-Export** (`UserData/ModDebugger-Report-*.txt`). Dazu ein Bridge-Eingabefeld, um Befehle direkt aus dem Fenster zu schicken. Eigener Prozess statt Ingame-Overlay, weil das Spiel sonst weiter die Tasten schluckt und der Screenreader weiter das Spiel liest — ein zweites Fenster erreicht man mit Alt+Tab und liest es im eigenen Tempo. Barrierefreiheit: **eine Spalte** (Screenreader lesen Zeilen, keine Raster), alles über Tastatur, und **„Live folgen" schaltet sich selbst ab**, sobald man eine Zeile von Hand auswählt — eine Liste, die ständig zur neuesten Zeile springt, kann man beim laufenden Spiel nie lesen. Live validiert: Strg+Y öffnet das Fenster aus dem Spiel, Transkript füllt sich, neue Zeilen kommen live an, Bericht liegt auf der Platte. **Offen für den Nutzer zu prüfen**: ob NVDA das Eingabefeld des Kommentar-Dialogs sauber ansagt (die UI-Automatisierung konnte es nicht lesen — kann Artefakt sein oder echt). (b) **Beim ersten Blick ins Debugger-Fenster fiel sofort auf, was im Log unterging**: Das Radio im Hostel liest **alle 12 Sekunden einen Wetterbericht für eine andere Stadt** — jede Zeile neu, also griff die Text-Wiederholungssperre aus #33 nie. Gefixt an der richtigen Stelle: Nicht der Text schleift, sondern die **Quelle**. Jede Orb-Quelle bekommt jetzt ein wachsendes Schweigegebot (erste Zeile frei, dann 15 s, 30 s, 60 s, bis 2 Minuten; nach 2 Minuten Ruhe wird ihr vergeben). Ein Radio redet sich damit selbst in den Hintergrund, eine Person, die einmal etwas sagt, wird nie gedrosselt. **Live gemessen**: Abstände wuchsen von konstant 12 s auf 24 s, 36 s und weiter.

  41. **Dachgeschoss beschrieben, Item-Beschreibungen ("was ist Glastara?"), Konfigurator-Datenverlust gefixt** (Commit `a25abc7` + folgende): (a) **Whirling-int-f3** besucht, angesehen, beschrieben — live validiert (*"Neuer Bereich: Whirling-in-Rags, Dachgeschoss"* + Beschreibung). (b) **Fünf Bereiche bleiben bewusst unbeschrieben**: Antechamber, Union-Container und die drei Traumszenen sind **per Teleport nicht ansehbar** — der Container rendert als überstrahlter weißer Blitz mit verzerrten Texturen (Beleuchtungs-Setup fehlt beim Teleport-Einstieg), bei den Träumen gehören die Marker nicht zur Szene und die Figur landet im Wasser. Eine Beschreibung aus einem kaputten Screenshot wäre eine selbstbewusst vorgetragene Lüge; sie bekommen ihren Text, wenn sie wirklich zu sehen sind. Träume brauchen ohnehin das eigene Konzept (volle, zeitabhängige AD — mit dem Nutzer abzustimmen). (c) **#31 Item-Beschreibungen** (Nutzer: *"und was ist Glastara?"*): Item-Namen sind Eigennamen aus einer Welt, die niemand von außen kennt — ein Sehender liest den Tooltip, ein Blinder hörte nur "Glastara, 6 Meter westlich". Die Mod liest jetzt die spieleigene `InventoryItem.description`; nach Nutzerentscheidung **beides**: Einstellung "immer mitlesen" (Standard aus, sonst begräbt sie jeden Raum-Scan) **plus Taste B** für den Abruf am ausgewählten Objekt (in allen Presets dieselbe, im Spiel freie Taste). (d) **Dabei einen stillen Datenverlust im Konfigurator gefunden**: Beim Speichern schrieb er nur `[KeyBindings]` und `[AccessibilityMod]` und **löschte alles andere** — die `[Tutorial]`-Sektion und unbekannte Einstellungen. Einmal Konfigurator öffnen und speichern hätte gereicht, damit **alle Tutorial-Tipps und alle Erstbesuch-Einführungen von vorn kommen**. Unbekannte Sektionen und Schlüssel werden jetzt unverändert durchgereicht.

  39. **Ladebildschirm-Hänger geknackt + Game-Tracer + Debug-Modus** (Commits `53bcda7`, `69e82d7`, Nightly `20260714121752`): (a) **Der Hänger war eine Exception, kein Timeout.** Neuer `tools/DevBridge/GameTracer.cs` (Unity-Log + Szenenladen + wer den Ladebildschirm offenhält, via `SceneTransitionManager.AddLoadingScreenDelay/Remove...`) hat ihn in flagranti erwischt: `NullReferenceException at Sunshine.Views.HudToggle.FixForDreamScene() ← SunshinePersistenceLoadDataManager.ResetVariousStates() ← LoadDataAfterLoadingArea() ← SceneTransitionManager.LoadSceneCoR`. **Eine Exception in einer Unity-Coroutine tötet die Coroutine** — der Rest des Ladevorgangs läuft nie, das Spiel steht für immer. Tritt nur auf, wenn geladen wird, **während das Spiel noch bootet**; ein Mensch trifft das nie (das Hauptmenü erscheint erst, wenn alles fertig ist), eine Bridge schon. **Gefixt** durch Warten auf dasselbe Signal, das der Mensch nutzt: `ViewController`-View muss `MAINMENU` sein (per Messung: bis ~43 s nach Start ist sie `SPECIAL`). Prüfen auf das `HudToggle`-Objekt reicht NICHT — es existiert während des Boots, ist nur unfertig, und genau darin kracht es. (b) Neue Bridge-Befehle: **`load <Namensteil>`** (gezielter Spielstand über `SunshinePersistence.Load`) und **`saves`** (volle Liste) — damit müssen nie wieder fremde Spielstände beiseitegeschoben werden, um den eigenen zum neuesten zu machen. Plus `trace on|off|tail|holders`. (c) **Debug-Modus** (Nutzerwunsch): eigener Haken im Konfigurator, unter dem jetzt **drei** Dinge hängen — Sprech-Protokoll, Strg+N (Namensquellen) und **Bildschirmwechsel-Ansagen inkl. der internen** ("Bildschirm: SPECIAL", "Bildschirm: MAINMENU"). Zweck (Nutzer): *"so kann ich dir sagen: im Bildschirm bla tritt ein Fehler auf"* — der Nutzer kann Fehler jetzt verorten, statt sie nur zu beschreiben.

  38. **NACHTSCHICHT 14.07. — Bereichsbeschreibungen, Karte, Dialogende, Diagnose-Taste** (Commits `deafd49`, `ff3c75e`, `f5da63b`, `bae8df3`; Nightly `20260714040752`):
      (a) **Bereichsbeschreibungen (Großaufgabe, Task #26)**: **25 Gebiete** haben jetzt je zwei feste Sätze (`mod/Settings/AreaDescriptions.cs`) — Satz 1: was für ein Ort das ist, Satz 2: wie er gebaut ist. Gesprochen beim Betreten (nach dem Bereichsnamen), wiederholbar mit **U**. **Jede** Beschreibung entstand, nachdem ich den Ort per Bridge angefahren, einen Screenshot gemacht und ihn **angesehen** habe — nichts aus dem Gedächtnis, weil eine plausibel klingende falsche Beschreibung bei einem blinden Spieler unbemerkt bliebe. Erster-Eindruck-Regel eingehalten: keine Story-Ereignisse, keine Figuren, die der Nutzer noch nicht kennt. Werkzeug dafür: `tools/DevBridge/tour.ps1` (zweistufiger Sprung — Szene laden, dann gültigen Ankunftsmarker holen, sonst landet die Figur im Nichts außerhalb des Levels). **Bereiche bekamen außerdem sprechbare Namen** ("Der Kiosk" statt "Pawnshop int"). **Nicht beschrieben** (ehrlich offen): `Whirling-int-f3` (Marker gehören zur Außenwelt, Figur landet im Nichts), `Whirling-int-f3-antechamber`, `Union-container-int` (Screenshot unlesbar), die Traumsequenzen (`Dream-*`, bewusst wegen Spoilern), `sense-of-scale`/`collageMode` (keine Spielorte).
      (b) **Karte per Tastatur (Task #28, erledigt)**: Die Reiseziele im Journal waren **maus-only** — Selectables, die die Pfeiltasten-Navigation des EventSystems nie erreicht. Jetzt sagt das Öffnen "Karte. 3 Reiseziele.", die Blättertasten gehen durch, die Interakt-Taste reist (über den echten Button, also gelten alle Spielregeln weiter). Am Tag-6-Stand validiert.
      (c) **"Dialog beendet."** — das Ende eines Gesprächs war stumm und klang genau wie "das Spiel wartet auf eine Antwortoption". Am globalen Signal des Dialogsystems, an der Badewanne live geprüft.
      (d) **Diagnose-Taste Strg+N (Task #25)**: sagt für das gewählte Objekt **alle** Namensquellen an (gesprochen / intern / Item-DB / Dialog-DB). **Bewusst nur Bericht, keine Umschaltung** — die Dialog-Quelle ist nachweislich falsch ("Kims Paperwork" → "Cuno"), eine Umschaltung würde dem Nutzer falsche Namen liefern, die er nicht prüfen kann. Nebenbefund: bei Objekten ohne Item-Namen liefert sie den **Gesprächstitel** ("WHIRLING / BATHTUB") — evtl. doch eine Namensquelle, noch offen.
      (e) **Vollzugriff (Task #27, erledigt)**: Ein Tag-6-Spielstand aus dem Netz (savegame.pro) lädt einwandfrei — **Stolperfalle**: Das Spiel liest den Zeitstempel aus dem **Dateinamen**, nicht aus dem Dateidatum; Umbenennen nötig. Neuer Bridge-Befehl **`lua <code>`** (`DialogueSystem.Lua.Run`) — voller Zugriff auf den Story-Zustand; der eingebaute Interpreter ist aber eingeschränkt (kein `pairs`). Der Spielstand enthält `states.lua` im Klartext: 128 `AreaState`-Einträge (33 Gebiete mit `HasEntered`, 89 Orte mit `LocationState`).

  37. **Installer-Selbstupdate war kaputt — Ursache gefunden, gefixt, und auf Nutzerwunsch mit Nachfrage** (Commit `0e675a9`, Nightly-Installer `20260713161306`; Meldung des Nutzers: "der Installer von ihr checkt nicht nach Updates. Auch datet er den Mod-Configurator nicht up."): (a) **Diagnose am echten Stick-Installer** (D:\, Standalone, Stand 12.07. 21:48): Er **prüft sehr wohl** — fand die neue Version, lud sie herunter, **tauschte sich sogar aus** — und meldete danach "Update-Prüfung fehlgeschlagen ()" (leere Klammer!) plus "keine Installation möglich". Sie bekam also eine Fehlermeldung von einem Installer, der sich gerade erfolgreich aktualisiert hatte, und kam nie zur Installation. **Das erklärt auch den zweiten Punkt**: Der Konfigurator wird ausschließlich aus der eingebetteten Kopie des Installers erneuert (SetupFiles-Selbstextraktion) — wer nie durchkommt, behält den alten. Der zweite Aufruf lief sauber durch (der Tausch war ja schon passiert). (b) **Gefixt**: Neustart fällt auf einen einfachen Prozessstart zurück, wenn die Shell verweigert; die **Mark-of-the-Web** wird von der geladenen Exe entfernt (genau daran scheitert `ShellExecute` bei Wechseldatenträgern); der **Download-Timeout stand auf 20 Sekunden für 68 MB** (Standalone!) → jetzt 10 Minuten; ein gescheiterter Neustart meldet "wurde aktualisiert, bitte erneut starten" statt "Prüfung fehlgeschlagen"; leere Exception-Messages nennen jetzt den Typ; `.old`-Reste werden bei jedem Start aufgeräumt. (c) **Nutzerwunsch**: *"ich will, dass er erst fragt … einfach im Hintergrund machen mag ich nicht."* → GUI **fragt jetzt vorher** (OK/Abbrechen, mit Begründung, warum das Update Pflicht ist); Ablehnen installiert nichts und sagt das. `--cli` läuft weiter automatisch (Konsole = alles sichtbar, muss skriptbar bleiben). Live getestet: veralteter Installer zieht sich sauber auf den neuen Stand (Exit 0). Stick trägt jetzt die reparierte Standalone.

  36. **Sprech-Protokoll als Nachweis + Frage "kann die Mod die Spielerin irgendwohin bringen, wo sie nicht hindarf?"** (Commit `d9b3a02`, Nightly `20260713155301`; Anlass: die Freundin des Nutzers hat evtl. etwas ausgelöst, das sie nicht wollte — "z. B. in die Badewanne"): (a) **Antwort auf die Sorge: technisch nein.** Die Mod **teleportiert nicht** — sie ruft `character.SetDestination(...)` (die spieleigene Wegfindung, echter Lauf über den Navmesh) und interagiert über `MouseOverHighlight.InteractFirstActive(ClickEventData)` — **derselbe Aufruf wie ein Mausklick**. Sie kann also nichts auslösen, was ein sehender Spieler nicht auch anklicken könnte. **ABER: Auto-Interact (Strg+F) macht aus "hinlaufen" ein "anklicken"** — bei Ankunft feuert die Interaktion ohne zweite Entscheidung. Das ist der realistische Hergang für "hab was getan, das ich nicht wollte"; wer das nicht will, schaltet Auto-Interact im Konfigurator ab. (b) **Nachweis gab es nicht** — Gesprochenes ging nur an den Screenreader, nichts wurde mitgeschrieben. Neu: `mod/SpeechLog.cs`, Schalter `SpeechLog` (**standardmäßig aus**, Checkbox im Konfigurator — Nutzer: "son Debug-Feature ists ja auch"), schreibt jede Ansage mit Zeitstempel nach `UserData/SpeechLog.txt`, pro Sitzung mit Kopfzeile inkl. Mod-Version. Eingehängt in `TolkScreenReader.SendSpeech` — dem einen Punkt, durch den alles Gesprochene läuft. **Zwei Bugs beim Live-Test selbst gefunden und gefixt**: eingereihte Ansagen landeten **doppelt** (laufen zweimal durch `SendSpeech` — einmal beim Einreihen, einmal beim Sprechen) → jetzt wird nur beim tatsächlichen Sprechen geschrieben, was auch die Zeitstempel ehrlich macht; und die Datei bekommt eine **UTF-8-BOM**, sonst zeigen Editoren "NÃ¤he" statt "Nähe".

  35. **Spieltasten-Nachschlageliste im Konfigurator + Karten-Taste geklärt** (Commits `894d11b` + `655fd2c`, gepusht; **Nutzerwunsch**: "ich fänds cool, wenn wir in der Tastenbelegungsliste auch die unveränderlichen Ingame-Tasten hätten, so zum Nachschlagen"): (a) Die Spieltasten lassen sich **nicht hardcoden** — wer im Spiel umbelegt, bekäme sonst eine Liste, die lügt; und in `Settings.json` stehen sie nicht (InControl legt Rebinds anderswo ab). Lösung nutzt vorhandene Substanz: Der `GameKeybindConflictChecker` liest die **live** InControl-Bindings ohnehin schon für die Konfliktwarnung — er schreibt sie jetzt zusätzlich nach `UserData/GameKeybinds.txt` (nach Aktion sortiert), und der Konfigurator zeigt sie **in derselben Liste** unter den Mod-Tasten an (ein Tab-Stop weniger als eine zweite Liste; Screenreader pfeilt einfach durch). Zeilen ohne Action-Tag = nicht änderbar: "Neu belegen" erklärt dort, dass die Taste in den Spieloptionen sitzt, statt stumm eine Belegung zu schreiben, die das Spiel nie liest. Auch im CLI: `--cli --list`. Live validiert (E=Interagieren, I=Inventar, J/P=Journal, C=Charakterbogen, T=Gedankenkabinett, Tab=Hervorheben, F1=Hilfe, F5/F9=Schnellspeichern/-laden, Leertaste=Stopp, 1–9=Dialogoptionen).
      (b) **Karten-Taste M — Befund per Fensterübernahme** (TTS-Protokoll eingehalten): Der Live-Dump zeigt `Map = m`, meine frühere Aussage "es gibt gar keine Karte" war also zu forsch. Test am Testspielstand: **M und J bewirken nichts** (Screenshot: normales Spielgeschehen). Gegenprobe beweist, dass es nicht an der Emulation liegt: **Escape öffnete/schloss das Pausenmenü**. Also **Spiel-seitige Sperre** — Journal/Karte/Inventar sind direkt nach dem Intro noch nicht freigeschaltet (deckt sich mit der Nachtschicht-Beobachtung, die ich damals als "Tutorial-Sperre" vermutet hatte). Die Spielhilfe listet "Karte" bereits. **Weiterhin offen**: an einem späteren Spielstand M drücken; Erwartung bleibt, dass die Karte kein View ist (kein MAP in der ViewType-Enum), sondern das Quicktravel-Overlay — das die Mod via `MapPatches.cs` schon abdeckt.

  34. **Bridge kann jetzt selbst spielen: springen, speichern, Screens öffnen** (Nutzeridee: "kannst du nicht einfach ein Level über Debug laden?" + "im Pause-Menü kann man ein Save auslösen, wenn du da ansetzen kannst, spart Token"): (a) **`goto <Szene> <Marker>`** nutzt die spieleigene Reisefunktion `AreaSpecificLuaFunctions.TeleportTo` (echter Übergang mit Ladebildschirm/Spawnpunkt/Kamera, kein Position-Hack) — **`scenes`** listet alle 36 Build-Szenen, **`destinations`**/**`travel`** die registrierten Reiseziele (`TravelDestination.ArriveAt`). **Wichtig gelernt: während eines laufenden Dialogs blockt das Spiel den Sprung stillschweigend** — erst nach Dialogende greift er. (b) **`view [TYPE]`** schaltet über `ViewController.GetViewByType` + `SwitchToView` direkt auf jeden Screen (INVENTORY/JOURNAL/CHARACTERSHEET/SAVE/LOAD/OPTIONS/…) — **kein Tastendruck, kein Fensterfokus**, damit sind Screen-Audits endlich ohne Fensterübernahme möglich. (c) **`save [name]`** über `SunshinePersistence.SaveWithScreenshot` — nötig, weil die Slots des Speicher-Screens **nicht EventSystem-navigierbar** sind (`ui`-Befehle erreichen sie nicht). Damit liegt jetzt ein Testspielstand **`martinaise-test`** draußen in Martinaise: kein Intro-Durchklicken mehr vor jedem Test, spart genau die Token, die der Nutzer meinte. **Live validiert**: neues Spiel → Intro → `goto Martinaise-ext main` → `save martinaise-test` → `saves` bestätigt.

  33. **Dialogoptionen + Bild-Buttons vertont, Bridge lädt nicht mehr ins Leere** (Commit `a454bfe`, gepusht, Nightly `20260713131934`): (a) **Ventilator-Dialog-Bug — vermutlich die wahre Ursache des "wir kommen aus dem Dialog nicht raus"**: Antwortoptionen wurden NUR vorgelesen, wenn das EventSystem eine markiert hatte (Controller/Pfeiltasten) — bei reinem Tastaturspiel ist nichts markiert, also totale Stille nach der Dialogzeile; dass überhaupt Optionen warteten, war nicht hörbar. Da Option 1 oft in dieselbe Zeile zurückschleift, drückt man blind Enter und hängt für immer fest. Neuer `mod/UI/ResponseListAnnouncer.cs` liest die komplette Liste nummeriert vor, sobald sie steht (0,35 s Settle gegen halbfertige Listen; queued, unterbricht die laufende Zeile nicht). Live validiert: *"2 Optionen: 1. Nie nie nie wieder? 2. (Du setzt einfach deine Nichtexistenz fort.)"*. Hinweistext sagt **Pfeiltasten + Enter** — die spieleigenen Ziffern-Shortcuts erwiesen sich im Test als unzuverlässig. (b) **Buttons ohne Text**: Der "BEGINNEN"-Knopf (allein auf schwarzem Schirm nach der Charaktererstellung) war für jeden Screenreader leer — Nutzer musste ihn per **OCR** erkennen. Ursache: `I2LocalizeButton` rendert die Beschriftung als **Sprite pro Sprache**, es gibt gar keine Textkomponente (per neuem Bridge-Befehl `inspect` nachgewiesen). `TextExtractor` fällt jetzt global auf den I2-Lokalisierungs-Term zurück (`Localize.Term` → `LocalizationManager.GetTranslation`); da der Term selbst nur ein Sprite-Name ist (`button-begin-de`), macht `NameFromSpriteTerm` daraus den Sinn ("begin") und benennt ihn über unsere eigene Loc-Tabelle (`UITerm_begin` → "Beginnen"). Gilt für jeden Bild-Button, kein Sonderfall. (c) **Bridge `loadnewest`/`quickload`** meldeten "triggered", obwohl gar kein Spielstand existierte → Spiel hing für immer im Ladebildschirm (Nutzerwunsch: "das sollten wir in der Bridge abfangen"). Fragt jetzt die spieleigene `SunshinePersistenceFileManager.GetLastSave()` und antwortet "no save games exist"; neuer Befehl `saves`. (d) **Haupt-Assembly nachdekompiliert**: `Assembly-CSharp` fehlte im 117er-Massenlauf, liegt jetzt in `disco-all/Assembly-CSharp` (1938 Dateien) — deshalb war die Save-API vorher nicht auffindbar.
      **BEGINNEN-Ansage live validiert** (Commit `d44c6d8`, Nightly `20260713134217`, lokal eingespielt): Erster Anlauf sprach weiter den rohen Slug — Ursache war nicht die fehlende Übersetzung, sondern dass die I2-Tabelle den **Sprite-Dateinamen als "Übersetzung" zurückgibt** (`button-begin-de`). Jetzt wird die Slug-Erkennung auf beide Seiten (Term *und* Translation) angewandt. Per Bridge neues Spiel bis zum Screen durchgeklickt: **"Button: Beginnen"**. Neue Regel vom Nutzer (als Memory gespeichert): **das Spiel darf jederzeit gekillt werden**, wenn es das Einspielen blockiert — kein Nachfragen.

  27. **Vier Spielbarkeits-Fixes nach Nutzer-Spielsession** (Commit `698b24f`, gepusht, Nightly-Assets inkl. beider Setup-Exes refreshed; Stick war beim Refresh nicht mehr angeschlossen): (a) **Dialog-Sperre**: Während die Dialog-UI aktiv ist (`SunshineContinueButton.instance.gameObject.activeInHierarchy` als zuverlässiges Signal — `isInConversation`-Flag bleibt in manchen Flows false) werden Navigations-/Kategorie-/Interakt-Tasten unterdrückt mit Ansage "In dialogue. Navigation is paused…" (max. alle 3 s); Dialog-Tasten (Lesemodus, Autoread, Wiederholen, Status-Ansagen) bleiben aktiv; Autoread nutzt jetzt dasselbe UI-Signal (fixt Intro-Flows). Nutzer war 5 Min. im Dialog durch die Gegend gerannt statt Enter zu drücken. (b) **Szenenwechsel-Ansage** ("Neuer Bereich: X", lokalisiert) — nach Nutzer-Regel (als Memory gespeichert: "nie für einzelne Szenen bauen, immer global") NICHT über Szenennamen-Liste, sondern global über "Spielercharakter existiert in der Szene" (Boot-/Menü-/interne Szenen bleiben mechanisch still; erste Szene nach Save-Load wird nicht angesagt). (c) **Türstatus-Ansage** open/closed/locked via `GetComponentInParent<Door>` (`_isOpen`/`_isLocked` aus dem Decompilat). (d) **Grobes Raumverständnis**: Kategorielisten überspringen Objekte, die der Autowalk nachweislich nicht erreichen kann (`IsReachable == false`; unknown bleibt drin). **Live validiert**: Boot still, Orte-Liste im Whirling von 9 auf 3 tatsächlich erreichbare geschrumpft, Badezimmertür korrekt "locked". Nebenbefund: F an der Tonband-Kiste öffnet ein Container-Panel ("Alles nehmen/Schließen") — vermutlich unvertont, prüfen. Dialog-Sperre am echten Tastendruck noch vom Nutzer zu testen.

  26. **Einzeldatei-Setup gebaut und auf USB-Stick gelegt** (Commit `0ff6ef7`, gepusht): Konfigurator, DevBridge und Tolk-Natives als EmbeddedResources in die Installer-Exe eingebettet, Selbstextraktion neben die Exe beim Start (schreibgeschützte Ziele werden still übersprungen → läuft auch vom Stick, dann ohne Sprachstatus). **Getestet**: Einzelne Exe in leeren Ordner kopiert → alle 4 Begleitdateien entstanden, CLI funktioniert. `DiscoElysiumSetup.exe` (682 KB) liegt auf `D:\` (Stick) und als drittes Asset an v0.2.0 + Nightly. Voraussetzung bleibt .NET-8+-Desktop-Runtime (kein Self-Contained — wäre ~150 MB).

  25. **Release v0.2.0 veröffentlicht** (Nutzer-Entscheidung "schmeckt nach v0.2.0" — im Einklang mit der No-Spam-Regel, da validiert): Tag war nach der alten Löschung wieder frei; basiert auf `4bc6041`; Mod-Zip + Tools-Zip (Installer, Konfigurator, DevBridge.dll, Tolk). Vollständige Release-Notes seit v0.1.0 (Keybinds/Presets, F-Interakt, Kategorie-Zyklus, Erreichbarkeit, Autoread, Auto-Interact, Sound-Captions, F1-Hilfe, Tutorial DE/EN, Tools inkl. CLI-Modi). Jetzt "Latest" → Installer-Standardkanal liefert v0.2.0; Nightly (gleicher Stand) bleibt als Pre-Release bestehen.

  24. **Tutorial end-to-end selbst durchgespielt + 5 dabei gefundene Bugs gefixt** (Commits `f0ee1b8`+`4bc6041`, gepusht, Nightly refreshed): Per Bridge eigenes Spiel gestartet (`ui down/submit` durch Hauptmenü → Denker-Archetyp → Intro "Uraltes Reptilienhirn"), danach Nutzer-Spielstand geladen und Ankunft validiert. **Alle 4 Tipps feuern genau einmal, auf Deutsch, mit Live-Tastennamen.** Gefixt: (a) `SpeakableName` machte aus F12 "F1 2" (Ziffer-Ziffer-Split); (b) `Loc` auto-Sprache war im Spiel immer Englisch — Unitys Il2Cpp-Domain meldet invariante CurrentUICulture, jetzt `GetUserDefaultUILanguage` (kernel32); (c) Gesprächs-Tipp feuerte nie — `isInConversation` bleibt in manchen Flows false (Intro verifiziert), Tutorial nutzt jetzt frisch gerenderte Dialogzeilen (`LineCounter`) als Signal; (d) **Erreichbarkeits-False-Positives behoben**: innerhalb 3 m = erreichbar, Partial-Path mit Ende nahe Ziel = erreichbar (Türen im Whirling jetzt sauber, Balkon korrekt weiter unerreichbar — live validiert); (e) Bridge-Spoken-Historie dedupliziert (Queue sprach doppelt durch Speak-Hook) + neue `ui selected/up/down/left/right/submit/cancel`-Befehle (Hauptmenü-Einträge sind keine Unity-Buttons — EventSystem nötig). **Aufgeräumt**: Nutzer-Lesemodus wieder Volltext, SeenTips geleert (Nutzer bekommt alle Tipps frisch). **Hinweis an Nutzer**: "WEITER" im Hauptmenü lädt jetzt evtl. MEINEN Test-Spielstand (neuester), eigene Stände über LADEN. **Offen**: Konfigurator-UI für TutorialTips/Language; Komplett-Lokalisierung (Folgeprojekt); `isInConversation`-Upstream-Bug tiefer fixen.

  23. **Tutorial-System v1 umgesetzt** (Commit `f0ee1b8`, gepusht; Nightly-Refresh + Konfigurator-UI noch offen): `mod/Settings/TutorialGuide.cs` (4 Einmal-Tipps: Welcome/Objects/Conversation/Arrival, Seen-Flags in `[Tutorial]`-Kategorie der Cfg, queued, `TutorialTips`-Setting) + `mod/Settings/Loc.cs` (erste EN/DE-Lokalisierung im Mod, `Language`-Setting auto|en|de). **Alle Tastennamen in Tipps live über `KeyBindings.SpeakableName`** (Nutzer-Regel bestätigt); nur F1 fest (Spielhilfe-Taste des Spiels, nicht remappbar). **Validierung per Bridge begonnen**: Welcome-Tipp feuerte beim ersten Start korrekt ([TUTORIAL] Tip played: Welcome im Log). Neue Bridge-Befehle `buttons`/`click <label>` — dabei gelernt: **Hauptmenü-Einträge sind KEINE Standard-Unity-Buttons** (nur Paperdoll-Slots gefunden) → für "Neues Spiel" braucht die Bridge noch einen EventSystem-Befehl (`ui submit/down/up` über ExecuteEvents auf `EventSystem.current.currentSelectedGameObject`). **Offen**: Bridge-Menü-Navigation bauen → eigener Spielstand → Objects/Conversation/Arrival-Tipps im Intro validieren; Konfigurator-Checkbox TutorialTips + Sprach-Dropdown; SeenTips in Nutzer-Config ggf. zurücksetzen (Welcome ist durch meinen Test verbraucht); Nightly-Assets refreshen; Komplett-Lokalisierung aller Alt-Ansagen (Folgeprojekt).

  22. **GEPLANT (mit Nutzer abgestimmt): Tutorial-System "an die Hand nehmen"** — Design gemeinsam entschieden: (a) **Nur Mod-Tutorial** (keine Story-Hilfe): kontextuelle Einmal-Tipps, ausgelöst vom Spielgeschehen über die bereits existierenden Patches — Begrüßung beim allerersten Start (3–4 Sätze); erstes Gespräch bei ausgeschaltetem Lesemodus → Tipp Vorlesen+Autoread; erste Objekte in Nähe ohne je Kategoriewahl → Tipp F2/Bild-Tasten/Strg+Pos1; erste Ankunft ohne Auto-Interact → Tipp F/Strg+F; erster Skill-Check, erstes Inventar, erstes Journal → je ein Tipp. Jeder Tipp genau einmal (persistierte Seen-Flags), immer queued (unterbricht nie), Präfix "Tipp:", abschaltbar im Konfigurator + Reset. (b) **Lokalisiert DE/EN** (Nutzer: "eigentlich hätte ich alles lokalisiert") → erstmals Loc-Infrastruktur im Mod (Key→En/De-Tabelle, Language-Setting auto|en|de, Konfigurator-Dropdown); Tutorial-Texte damit; **Komplett-Lokalisierung aller bestehenden Mod-Ansagen als separates Folgeprojekt** (hunderte Strings). (c) **Validierung: Ich spiele selbst per DevBridge** — eigener Spielstand über "Neues Spiel" (genehmigt, Nutzer-Saves unberührt), Anfangsszene fernsteuern, per spoken/screenshot prüfen, dass jeder Tipp im richtigen Moment kommt; Menü-Navigation braucht evtl. neue Bridge-Befehle (UI-Pfeile/Submit). Tasks #11–#13 angelegt, Umsetzung noch nicht begonnen.

  **Noch offen:**
  - **Erreichbarkeits-Check produziert False-Positives** (Live-Log: Türen 1–6 m entfernt als "not reachable" markiert, vermutlich NavMesh-Snap auf falsche Seite der Wand) — nachjustieren, z. B. "Pfad endet nah genug am Ziel" als erreichbar werten.
  - **AI-Spiel-Schnittstelle (DevBridge)**: Nutzer-Idee, von mir befürwortet — separate Dev-Mod unter `tools/DevBridge` (nicht im Release) mit lokalem Kommandokanal (GameKey simulieren, Objekte/Dialogzustand abfragen, letzte TTS-Ausgaben lesen), damit die AI selbst testen/spielen kann. Noch nicht begonnen.
  - Interaktive Live-Tests durch Nutzer: **F an einem Objekt** (z. B. dem Schuh), **Strg+Bild↓/↑ Kategorie-Wechsel**, Autoread im Gespräch (G bei Volltext-Modus), F1-Hilfe, Sound-Captions.

  20. **Release-Politik umgestellt (Nutzer-Regel, als Memory gespeichert: kein Release-Spam)**: v0.2.0/v0.3.0/v0.4.0 samt Tags gelöscht (waren alle vom selben Tag, ungetestet). Stattdessen genau EIN bewegliches Pre-Release **`nightly`** ("Nightly (untested snapshot)", aktuell Commit `7aebd27`, Mod- + Tools-Zip als `-nightly.zip`), das bei neuen Ständen in-place aktualisiert wird (Assets ersetzen, Tag umsetzen — kein neuer Release-Eintrag). v0.1.0 bleibt "Latest", darauf zeigt auch der Installer standardmäßig (`/releases/latest` ignoriert Pre-Releases). Versionierte Releases erst wieder, wenn der Nutzer durchgespielt/getestet hat.

  21a. **Auto-Interact + AI-Dev-Bridge + Installer-Option + Konfigurator-Umbenennung** (Commit `f088ae2`, gepusht, beide Nightly-Assets in-place ersetzt): (a) **Auto-Interact** (Strg+F-Toggle in allen Presets, Setting `AutoInteract`, Konfigurator-Checkbox): nach Ankunft des Autowalks an einem Objekt wird automatisch interagiert (Wegpunkt-Ankünfte ausgenommen; Hook über `MovementController.OnMovementCompleted`). (b) **tools/DevBridge**: Dev-Companion-Mod, dateibasierter Kommandokanal `UserData/DevBridge/command.txt`→`response.txt` (5-Hz-Poll); treibt die Haupt-Mod über neuen public-static-Accessor `AccessibilityMod.NavigationSystem` (kein Key-Simulieren); TTS-Verlauf via Harmony-Postfix auf `TolkScreenReader.Speak` (dafür public gemacht). **Live selbst getestet: kompletter Kreis Senden→Handeln→Hören (spoken)→Sehen (screenshot per Read-Tool ausgewertet, Hauptmenü erkannt)**. Quickload via `SunshinePersistence.DoQuickLoad` (erster Versuch mit SaveLoadController scheiterte — FindObjectOfType findet das inaktive View nicht). Nie im Mod-Zip, nur im Tools-Bundle. (c) Installer: Checkbox "AI-Dev-Bridge aktivieren" + `--devbridge`/`--no-devbridge` (kopiert/entfernt `Mods/DevBridge.dll`). (d) KeybindEditor → **DiscoElysiumModConfigurator.exe** umbenannt (Nutzerwunsch: "vollwertiger Konfigurator"; Fenster-/Verknüpfungs-/Button-Texte angepasst, Locator findet auch den alten Exe-Namen, Projektordner bleibt `tools/KeybindEditor`). Config-Sync per Editor-CLI ergänzte `ToggleAutoInteract`. Kosmetik-Rest: alte Startmenü-Verknüpfung mit altem Namen bleibt liegen, bis der Installer neu läuft.

  21. **Installer: Nightly-Kanal + Spiel-läuft-Schutz** (Commit `b18eb87`, gepusht; Nightly-Tools-Asset in-place ersetzt): (a) Nutzerwunsch: Checkbox **"Aktuellstes Prerelease laden (Nightly)"** in der GUI + `--prerelease` im CLI — geht die volle Release-Liste durch (neuester non-draft Eintrag) statt `/releases/latest`; Standard bleibt Stable. **Live getestet**: CLI mit `--prerelease` zog korrekt "nightly" statt v0.1.0. (b) Dabei UX-Lücke gefunden und gefixt: Installation bei laufendem Spiel schlug mitten im Kopieren mit roher File-in-use-Exception fehl (DLL vom Spiel gesperrt) — jetzt bricht der Installer vorab mit klarer lokalisierter Meldung ab ("Disco Elysium läuft gerade — bitte zuerst das Spiel beenden"); auch das live getestet, da das Spiel beim Test tatsächlich lief.
  - Echtes Durchspielen/Dauertest mit den neuen Tasten (bisher nur kurze Rauch-Tests beim Neustarten).
  - PR-Inhalt für Upstream (`game-a11y/Disco-A11y`) entscheiden — Nutzerwunsch: erst ein **Issue** aufmachen (Hotkey-Problem melden, auf unseren Fork/PR als Lösung verweisen, abwarten was der Maintainer dazu meint), nicht direkt ungefragt einen PR aufdrängen. Frage nach GitHub-Actions-Release-Automatisierung fürs Fork ist geklärt: nicht praktikabel, da der Build echte Il2Cpp-Assemblies aus dem lizenzierten Spiel braucht, die eine CI-Runner nicht haben kann — bleibt bei lokalen Releases.
  - Größere Idee (noch nicht begonnen): eigener "Stardew-like Navigation"-Modus als Alternative, nicht nur einzelne Tastenübernahmen — z. B. Kachel-Cursor-Navigation wie stardew-access' Tile Viewer. Deutlich größerer Umbau als reines Rebinding.
  - **Backlog (zurückgestellt, "wenn mal zu viel Zeit ist"): "Fehlerbericht senden"-Knopf.** Nutzer sollen ohne GitHub-Konto/USB-Stick berichten können. Erfassung existiert schon (Debugger-Report mit Zeilen-Kommentaren); es fehlt nur der Transport. Entschiedenes Design: Knopf im **Debugger** → simpler HTTPS-POST an eine **Serverless-Function** (Cloudflare Worker o. Ä., Gratis-Kontingent, ~30 Zeilen), die mit *unserem* fein-granularen GitHub-Token (nur Issues, nur Fork) serverseitig ein **Issue anlegt** → du liest's in der Issue-Liste, ich per `gh`/WebFetch. Kein Nutzerkonto, Token nie beim Nutzer. **GitHub Pages scheidet aus** (nur statisch, Token wäre im Browser öffentlich). Auto-Kontext anhängen (Mod-/Installer-Version, OS, Screenreader erkannt?, letzte Sprech-Zeilen, Szene, Config) + Freitext "Was ist passiert?" + Bestätigung vor Senden (Bericht enthält Spielstand-/Szenentext) + Tolk-Ansage. Offline-Rückfall: Bericht als Datei speichern (jetziger Weg). Braucht von Danijel einmalig: Gratis-Host-Konto + Issues-Token.
  - `tools/KeybindEditor/GameKeyCatalog.cs` ist eine von Hand gepflegte Kopie von `mod/Settings/KeyBindings.cs`s `GameKey`-Liste (Tool kann das MelonLoader/Il2Cpp-Mod-Projekt nicht direkt referenzieren) — bei künftigen `GameKey`-Änderungen beide Stellen synchron halten.
